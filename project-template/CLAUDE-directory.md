# <!-- Directory name -->

<!-- Copy to a directory that wants its own rules but is NOT a project — notes, a catalog,
a scratch or playground area. There is deliberately no `## Project` section: that section is
what makes a directory a project, and without it Flow's ticket pool, `docs/` tree and `flow`
command do not apply here. Add it later if this turns into real work. -->

## Rules

> How work here differs from the global defaults. Conventions, what not to touch, and any
> path Flow would otherwise guess — every path Flow names is a default, and one named here
> wins for this directory.

<!-- Everything else — the user profile, judgment, explaining, capture, hard rules — is
global, at ~/.claude/CLAUDE.md, and loads automatically. If it isn't there, run
setup-flow-globals from the flow repo. -->

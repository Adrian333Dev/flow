# Changelog — brainstorm

## 2026-07-23

- Companion-skill references updated for the `context-capture` → `capture-context` rename.
- Sub-file `writing-plans.md` renamed `write-plan.md` (verb-first naming, parallel to `write-spec.md`); references updated across SKILL.md and write-spec.md.
- `write-plan.md`: skill references updated for the `executing-plans` → `execute` rename.
- Initial publish (moved from the agentic-workflow template; renamed from `brainstorming`). Branch-tree flow with two sub-files: `write-spec.md` (spec synthesis) and `writing-plans.md` (implementation plan — formerly a standalone skill). Phased-output rules (tree never shown; orient and first branch question are separate messages; lightweight tree-less mode), locked-decisions-only batched writes, plain-prose branch walking.

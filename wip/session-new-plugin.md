# Session — New Agentic Workflow Plugin

## READ THIS FIRST — state as of 2026-08-07

> **This file is the historical log — read it third.** `wip/handoff.md` is where things stand and what to do
> next; `wip/remaining.md` is the master checklist of everything left to build, in build order, each item
> pointing at where its design lives. Come here for *why* a decision was made, not for what to do.
> (`backlog.md` was fully absorbed into `remaining.md` and **deleted 2026-08-07** — do not look for it.)
>
> ### Where everything is now — the repo moved 2026-08-07
>
> **`flow` is the only repo. The `agentic-setup` workbench is deleted, GitHub repo included.** Everything
> worth keeping was carried into **`wip/`** at the flow root: this file, `remaining.md`, the six
> `design-*.md` files, `rejected-init-flow/`, `user-profile.md`, plus `v1-template/` and `framework-build/`
> (the previous generation) and `study-cases/`, `research/`, `archived-skills/` (the evidence behind the
> skills). `proxy.mjs` is there too — a dev-only context auditor, deliberately **not** a shipped script.
> `excalidraw/` holds three third-party diagram skills with a README comparing them; they are kept for a
> verdict, are **not** Flow skills, and `explain`'s SVG/mermaid ban never covered them.
>
> **`wip/` is scaffolding and gets deleted when the build is done.** Nothing in it installs. Never let a
> `wip/` path leak into a skill, into `global/`, or into `project-template/`.
>
> **`toolbox/` is a submodule** — [`Adrian333Dev/toolbox`](https://github.com/Adrian333Dev/toolbox), still
> its own independent repo. `global/CLAUDE.md` now points at `~/.claude/toolbox`, a symlink that
> `setup-flow-globals` must create. Clone with `--recurse-submodules` or the folder comes down empty. Never
> edit it as if it were part of this repo.
>
> **Older entries in this file still name `new-workflow/` and `agentic-setup/` paths.** This log is
> newest-at-the-bottom and those are dated records, not live pointers — read them as history. Only the block
> you are reading now is current.
>
> ### State as of 2026-08-07 (end of session)
>
> **The `flow` command is built and working** (2026-08-06/07) — `flow/global/scripts/flow/`, ~950 lines,
> zero dependencies, every command in the confirmed surface. Four PATH commands are live on this machine:
> `ptree`, `fmerge`, `gsave`, `flow`, each a symlink in `~/.local/bin` pointing at an extensioned file in
> `flow/global/scripts/`, plus a folder symlink `~/.claude/scripts`. The `PreToolUse` guard is **not**
> installed — `~/.claude/settings.json` still has no `hooks` key; that is `setup-flow-globals`' job.
>
> **Done this session (2026-08-07):** `## 2h` — `flow/global/CLAUDE.md` rewritten for the ticket chain
> (`## Key docs` rebuilt to eleven rows, `## Workflow` rewritten, `## Capture` rewritten, flow-notes row
> added). `## 2j` — the project template is now two files. Flow-notes moved to **`~/.claude/flow/notes.md`**.
> The router skill is **dropped** (it orients newcomers; there are none — a README or guide covers it if
> Flow ever opens up). `backlog.md` and `flow/project-template/docs/` both deleted. Then the repo migration
> above: `wip/` created, `toolbox` added as a submodule, and `global/CLAUDE.md`, `README.md`, `CLAUDE.md`
> and the `setup-flow-globals` spec all updated for the new toolbox path.
>
> **⏸ ONE THING IS PARKED AND IT BLOCKS THE SKILL REWRITES.** `remaining.md` opens with a section titled
> *"PARKED — delete the topic; a ticket holds its own brainstorm"*. It is a fully-worked proposal the user
> understood and **deliberately did not approve**; he wants other work first. **Do not implement it and do
> not re-open it unprompted.** But note that `## 2c`–`## 2g` (the five skill rewrites) are all written
> against topics, so building them before that call is made risks doing the work twice. Raise the ordering
> once, then follow the user's answer.
>
> **Next action — the user's call between:** (a) restructure `remaining.md` itself (split steps 4–5 out to
> `wip/migration.md`; split every checkbox into **must** / **later**; cut settled argument out of
> `[x]` items) — approved in conversation, not yet done, spec at the bottom of `remaining.md`;
> (b) build step 3, `setup-flow-globals`; or (c) un-park the topic decision.
>
> ### Two things recorded 2026-08-07 and deliberately not opened
>
> 1. **Skills vs. agents vs. commands** — Claude Code has three extension points and Flow reflexively uses
>    skills for everything. Never actually decided.
> 2. **Managing a growing set of global scripts**, most of which are *not* Flow-specific (`gsave` is the
>    model). Where general tools live, how they register, whether Flow should own them at all. The `toolbox`
>    submodule is a partial answer for *catalogued* tools; it says nothing about where scripts live.

**This file is newest-at-the-BOTTOM.** Everything below this header is older than it. The authoritative design record is `new-workflow/design-init-flow.md` → its last two sections, **`## SESSION 2026-08-02 — Flow goes global`** then **`## SESSION 2026-08-03 — one repo`**. Read both before acting on anything; 08-03 supersedes parts of 08-02.

**One repo — 2026-08-03. Build step 1 is DONE.** `flow-skills` merged into `flow`: the eight live skills now sit at `flow/skills/` (the rejected ninth, `init-flow`, is parked in `new-workflow/rejected-init-flow/`), the global install at `flow/global/`, the new-project scaffold at `flow/project-template/`. `flow` is no longer a GitHub template repo (its "Use this template" button would have copied skills and global config into every new project) — it is the system repo, cloned once per machine. The skills CLI, `plugin.json`, the README registry and versioning are all dead, replaced by `global/scripts/link-skills.sh`. **Cleanup done 2026-08-03:** `flow-skills/` (local folder and GitHub repo), `flow/recommended-tools.md`, `flow/docs/`, and `reference/archived-skills/` are all deleted — do not look for them and do not re-create them.

**Permission model settled 2026-08-03 — designed, NOT installed.** The constant "may I run this command" prompts are solved *in the template only*. `flow/global/settings.json` now carries the finished shape: a `permissions.allow` of bare tool names (`Bash`, `Edit`, `WebFetch`, `mcp__context7__*`) so no list of command strings ever has to be curated; the 32-entry `permissions.deny` unchanged; and a `hooks.PreToolUse` entry running `global/scripts/guard.py` on every Bash call. New file **`flow/global/settings.md`** documents every key — `settings.json` is strict JSON, so comments are impossible and the docs have to live beside it. Full reasoning, including why auto mode and the bubblewrap sandbox were each considered and rejected, is `design-init-flow.md` **#H9**.

**Two hard constraints, both stated emphatically by the user:** (1) the blanket allow and `guard.py` are **one unit and never ship apart** — allow-without-guard leaves only the deny list; (2) **nothing gets merged into `~/.claude/` until the whole repo is finalized.** The user added two narrow `tree.sh` / `merge-files.js` allow entries to his own settings by hand as a personal interim fix; that is not Flow's config and must not be mirrored into the template. Do not propose installing any of this early. Separately: **"Flow ships no hooks" was never a decision** — the agent invented it in #H8; it is withdrawn there and Flow ships a `PreToolUse` hook.

**Three things settled there that keep getting re-litigated — do not reopen:** (1) every global file has a *template* version in the repo and a *personalized* version at `~/.claude/`, they diverge on purpose, and backing up the personal one is the user's private-repo business, not Flow's; (2) the `---` divider and the whole "re-run the installer to update" path are retired permanently; (3) skills carry changelogs for behavior changes only, and carry no version numbers.

**No Flow skill is installed globally.** The stale `~/.claude/skills/` links into `flow-skills/` were removed 2026-08-03 and nothing was linked in their place — deliberate, the skill set is not finalized. Treat `flow/skills/` as plain files being edited; do not link them, and do not assume any Flow skill is invocable. `link-skills.sh` runs once, when the set is final.

**Flow goes global — the big reframe, 2026-08-02.** The user rejected the built `init-flow` skill outright and the whole front-of-lifecycle inverted. In one paragraph: brainstorming a product happens **anywhere, with no repo and no setup**, because commitment to a project is an *output* of the brainstorm, not a precondition. So almost all of `flow/CLAUDE.md`, all three scripts, and the git deny list + SessionStart hook relocate to `~/.claude/` where they load in every directory. A project's own `CLAUDE.md` keeps exactly two sections — `## Project` and `## Project rules` — and the payload collapses from six files to two.

**Consequences, all user-confirmed:**

- **`init-flow` is dead as designed.** Its SKILL.md (196 lines, written 2026-07-31) is **rejected in full — do not patch it.** Parked at `new-workflow/rejected-init-flow/` as the input to its rewrite. It becomes `migrate-to-flow`, scoped to the one case that actually needs a skill: an existing codebase. Empty repos and docs-only repos need no skill at all ("Use this template" or `git clone` plus the published guide).
- **New primary skill: `setup-flow-globals`** — one-time per machine. Writes the global `CLAUDE.md` + scripts + settings, interviews once for `## The user`. Product-mode brainstorm checks for the profile and *redirects* to it; it never does that work inline.
- **Auto memory can't hold the user profile** — it is per-repository and machine-local. Verified: `~/.claude/settings.json` still has `autoMemoryEnabled: true` and two projects (`backmark`, `backmark-validation`) have live memory dirs. One-line fix, belongs with the global settings move.
- ~~**`recommended-tools.md` now ships** into projects~~ — **superseded 2026-08-03** (`design-init-flow.md` #H6): the file is deleted, its content lives in the separate `~/code/toolbox/` repo, and `global/CLAUDE.md` points at it. `README.md` still doesn't ship.
- **No interview skill.** Matt's `grilling` is seven lines and `brainstorm` Phase 2 already contains all of it.

**Build order — do these in order:**

1. ~~**Restructure `flow/` for the global split.**~~ **DONE 2026-08-03** — see `design-init-flow.md` #H7 for the built layout.
2. **Rebuild the brainstorm→ticket→plan chain — SHAPE SETTLED 2026-08-05.** Read `design-brainstorm-rework.md` → **`## SESSION 2026-08-05`** first and treat it as authoritative; it supersedes every structural proposal in `## SESSION 2026-08-04` and most of the LOCKED #1–#8 block above it. One-paragraph summary: **topics** (`docs/topics/<slug>/` — `topic.md`, `brainstorm/tree.md` + one detail file per root branch, `research/`, `proto/`) are the only place a brainstorm lives; **tickets** are one flat global pool at `docs/tickets/tNNN-slug.md` with frontmatter `id/title/status/topic/deps` and statuses `todo|in-progress|review|done|dropped|superseded`; there is **no spec file and no index file** — `flow tickets --ready` computes what's next; the **plan is a `## Plan` section inside the ticket**, written at pickup, opening with a pass that examines the current state of the code, and carrying code only where the code *is* a decision. A ticket is unopened, broken-down (a topic points at it), or planned, and which one it becomes is judged at pickup, never in advance. `brainstorm`, `write-spec.md` and `write-plan.md` all get rewritten; `execute` is implicated via its single-plan-file dependence; the profile-existence check that redirects to `setup-flow-globals` still belongs somewhere in this chain. **PRODUCT MODE SETTLED 2026-08-06 — see `## SESSION 2026-08-06`, authoritative over everything above it.** Product mode uses the **same engine** (the tree — proven by real-aloud, a full product brainstorm that ran as an ordinary tree); it differs only at the exit. Brainstorm lives at `docs/brainstorm/` (`tree.md` always whole + a detail file per branch that actually grew) — this **supersedes `design-init-flow.md` #1b's `docs/work/brainstorm.md`**. Output is `docs/spec/`, **markdown only**, base = `product.md` (the Bible: whole product, all behaviors, plus the V1/next/later/never **scope ladder**) + `tech.md`; more files are expected (`design.md` for visual products) but must pass **disjointness — no fact in two files, boundary statable in one sentence**, the rule derived from lumacraft's overlap failure. **No `decisions.md`, no `open-questions.md`, no README index, no frontmatter** — the tree is the decision log and open questions attach to the ticket that will answer them. **Artifacts are referenced, never copied**: `proto/<name>/` at repo root (flat, no grouping by kind), `docs/research/` **global** (this supersedes the 08-05 per-topic `research/`), cited inline plus a reference block per spec file, and inherited by the ticket minted from that section. Tickets are minted **only from the V1 rung**. **One skill, two exits**, default topic mode, product mode entered explicitly — the sub-file is about the conversion, not the brainstorming. Reversals to honour: "ticket" as vocabulary is back in, modes are permitted again, Wayfinder is fully out, `issues.md` is not carried forward. **`tmp/prod-brainstorm/` folders are evidence, never templates** — `real-aloud-app/` is the best case, `lumacraft-spec/` is the failure. **Also locked 2026-08-06:** ticket **`type` reinstated with four values** — `feature` / `issue` / `chore` / `research` (the 08-05 removal killed six *agent-invented* types; the user's reason is filtering, and `research` has a real behavioural difference — it always spawns thinking, never a direct plan); **`protos/`** plural at repo root; **zero-based branch indices** (`0`, `1`, `2`; children `0.0`, `0.1`) replacing letters, with detail files prefixed `0-product.md`; **`~/.claude/flow-notes.md`** as the single global home for notes about Flow itself (per-project files argued down and dropped; needs a `## Capture` row and a `setup-flow-globals` overwrite-exclusion — both in `backlog.md`). **`setup-notes.md` is a delapse file, dead in Flow — never a destination.** **The scripts are now the blocker** for the ticket system being usable (`backlog.md` → `## Scripts`), and **`prototype` needs its own brainstorm before it can be built**. **SECOND SESSION 2026-08-06 — the scripts are now designed and the blocker is cleared; full detail in `new-workflow/remaining.md`.** Locked there, all user-confirmed: (a) **a ticket is a FOLDER from birth** — `docs/tickets/t047-slug/` holding `ticket.md` (constant name, like `SKILL.md`), `handoff.md`, and one `<slug>.md` per dispatched job brief; this **reverses 08-05's flat-file pool**, and promote-on-status was separately proposed and rejected because location would duplicate `status`; (b) the **08-05 command list was never really approved** and is a rejected draft — the confirmed surface is `flow next / start / review / done`, `flow ls [status] [--type] [--topic]`, `flow show`, `flow status`, `flow check`, plus namespaced `flow ticket …` and `flow topic …`; (c) **JavaScript**, zero deps preferred but not mandated, frontmatter hand-parsed; (d) **root discovery via `git rev-parse --show-toplevel`** — commands take no path, `.git` is the marker (not `docs/tickets/`, which does not exist before the first ticket); (e) templates are **real files beside the script**, never embedded strings; (f) general tools renamed **`tree.sh` → `ptree`**, **`merge-files.js` → `merge`**, registered on `PATH`, with `flow` as the only Flow-namespaced command; (g) **frontmatter is owned by the commands, everything else is written by hand** — the enforceable form of "commands are mandatory"; (h) **`docs/work/` is deleted** — `docs/inbox.md`, `docs/handoff.md`; **`backlog.md` is dropped**, its capture row pointing at the inbox, and `organize` gains "mint a ticket" as a destination; (i) **dispatching a subagent is an execution choice, minting a ticket is a recording choice — independent**; a resume handoff and a job brief are different documents, and the shipped `handoff` skill already draws that line; (j) the **`debug` skill is missing entirely** and was never designed; (k) open at scale: whether terminal tickets get archived — recommendation *not yet*. **`design-init-flow.md` #B (the `status.sh` topic sweep) is superseded** by `flow status` / `flow ls`.
3. **Write `setup-flow-globals`.**
4. **Write `migrate-to-flow` last**, against a destination that by then exists. Then test it on `delapse` / `lumacraft_v2`.

Also open, unblocked: ecosystem branch #6 (skill-creation trigger); the rest of `new-workflow/backlog.md`.

**One repo, one name:** `github.com/Adrian333Dev/flow`. The catalog repo `flow-skills` was deleted 2026-08-03 — any reference to it below is a dated record, not a live path.

**Vocabulary used across these docs** (define it, don't assume): *harvest* = lift individual facts out of the user's existing files and discard the rest; *payload* = the template files copied into a project — now two, was six; *quarantine* = move colliding existing material aside before writing Flow's layout, never merge with it; *consolidation* = the retired internal name for turning scattered material into a finished `docs/spec/`, work that belongs to `brainstorm`'s full-product mode.

---

_Last updated: 2026-07-24 (session 14e). **Repo reorg done** — this dir is now a **container** holding `agentic-workflow_v2/` (the template, renamed from `agentic-setup_v2`, own git repo) + `skills/` (new skills catalog, own git repo); design docs stay in `new-workflow/`; v1 + `framework-build` + external clones archived to `reference/`. See the **"Latest"** section directly below. TWO design threads: (1) **`project-init` / front-of-lifecycle** — `new-workflow/design-project-genesis.md` — essentially CLOSED (branches #1–#5, #8 resolved; #6 topology parked). (2) **Skill & knowledge ecosystem (#7)** — `new-workflow/design-skill-ecosystem.md` — **branches #1–#4 CLOSED, branch #5 (init-integration seam) is next but PAUSED** — session 12 pivoted to a battle-test-driven overhaul of the shipped skills (communication/`/copy` rules, visualization U-turn to text+ASCII, memory retired, `research-evaluation` removed). See **"Latest — session 12"** below. `agentic-workflow_v2/` is the primary artifact; live skills (session 14b): the **catalog** holds all six — `brainstorm` / `research` / `explain` / `capture-context` / `debug-web-pages` / `execute` (global via `~/.claude/skills` symlinks); the **template** ships ZERO skills._

**Scope note:** this file tracks work under `new-workflow/` only. Do NOT edit `framework-build/` files without explicit instruction. **Relationship clarified 2026-07-01 (session 2):** `new-workflow/` is effectively framework-build v2 — same goal (replace the rigid Superpowers workflow), different method (curate/adapt existing Claude/Codex-compatible skill ecosystems instead of building a from-scratch guide system). `framework-build/` may be fully rewritten or abandoned — not certain. Its locked decisions (`framework-build/docs/design-session.md`, 85 decisions) are now treated as a **reference/source of validated thinking** to pull into new-workflow — read freely, cite freely, port decisions over. Just don't *write* to framework-build/ files without being told to.

---

## Latest — 2026-07-24 (session 14e): capture/knowledge operational layer REDESIGNED — inbox model + `organize` / `curate-skills`

Walked the 14d adversarial-pass positions one issue at a time (used `/explain` per user request — earlier framings were "too complicated"). Core skill-as-knowledge-unit model survives; the **operational layer is redesigned** around a single inbox + two commands. **Everything below LOCKED.**

- **One ambient behavior holds** — only `capture-context` runs passively. Issue #1's "make capture dumb / one line / zero-routing" is SUPERSEDED by the inbox model (same goal, achieved better). Capture is also **relaxed**: not every-turn — a light occasional reflex + user-promptable ("note that," "save that pattern"). Overload concern resolved by this + moving all heavy work (routing, skill create/edit) off the ambient lane.
- **Inbox model (replaces the rejected `learnings.md` AND capture-time routing).** During work, everything worth keeping — decision, finding, idea, reusable knowledge, all mixed — dumps RAW into ONE file **`docs/work/inbox.md`** (name changeable), scoped to the active topic/milestone. No destination decision at capture time — that was the whole win (kills the "fits three files at once" problem the user flagged). Exception: an active `brainstorm.md`/`spec.md` still owns its own in-progress content; inbox catches everything else surfacing alongside.
- **Routing relocates from capture → `organize`.** capture-context's current capture-time routing table (decision→decisions.md, finding→notes/<topic>.md, idea→backlog.md, pref→preferences.md) MOVES out of capture and into the `organize` command. Capture just appends to inbox. **This revises the shipped `capture-context/SKILL.md`** — its "Where to write" / "How to write" tables migrate to `organize` (not yet executed — awaits an approved plan).
- **Two commands, split by DIRECTION:**
  - **`organize`** (inbound; frequent) — reads inbox, distributes each item to its proper home (project files + a skill's knowledge). Appends reusable knowledge as a line to skills that ALREADY exist. Replaces old "sort." Agent offers it ("full inbox — organize it?") + user-invocable.
  - **`curate-skills`** (works ON the library; occasional/deliberate) — create a new skill, restructure one, prune stale knowledge. Name LOCKED over `skillify`/`curate`/`janitor` (must contain "skill" for clarity; "curate" keeps full create+prune+tend scope). May lean on existing `write-a-skill`.
  - Dividing rule: **adding a line = `organize`; building/reshaping a skill = `curate-skills`.**
  - Handoff: when `organize` finds reusable knowledge with NO home skill, it does NOT build mid-flight — flags it ("needs new skill: X"); `curate-skills` builds it deliberately later. (Replaces Branch #3's "stub-on-the-spot.")
- **Trigger (issue #2 RESOLVED).** Guaranteed moment = the **review step**, which recurs after EACH artifact (brainstorm/spec/plan/execute), not just post-execution — because U-turns/loop-backs can happen at ANY stage (flow must be maximally flexible: after any artifact the user may approve, loop back, or drop it entirely). At review, `organize` is a **surface-and-offer** ("N items staged — organize now?"), **deferrable**; inbox **persists across any U-turn/loop-back**, so nothing is lost even on a full revert (knowledge learned building a reverted feature is still valid). Exact wiring deferred — **the review/finalize phase is itself undesigned; it owns the organize trigger** (new deferred thread).
- **Audit parked as issue #4** — checking current work against a skill's accumulated best-practices is a DIFFERENT function (using a skill to review, not maintaining it); explicitly OUT of `curate-skills` scope; design later WITH scoping (must be scoped to skills the work touched — unbounded reads otherwise).
- **Supersedes in `design-skill-ecosystem.md`:** Branch #3 (Notice/Sort loop, stub-on-the-spot) reshaped into inbox + `organize` + flag-not-stub; "no global inbox" still holds (inbox.md is per-project + temporary, not a cross-project store). **DONE (2026-07-24):** folded a dated *Superseding Update* section into `design-skill-ecosystem.md` (before Branch #2) marking Branch #2/#3/#4-pt3 superseded. Also tidied `new-workflow/`: deleted now-redundant `hard-rules.md` (all 4 rules already in `flow/CLAUDE.md`), and trimmed the collapsed `#7` historical block from `design-project-genesis.md`.

**RESUME POINTER (14e).** Operational-layer redesign LOCKED (above). **Issue #3 RESOLVED — no taxonomy.** User rejected the fact/stance/open-question split as overcomplication: a skill is just **best-practices + tips + guides in natural prose** (model = `debug-web-pages` — a loop + modes + `knowledge/` files of accumulated dated findings, zero tags). "Stances" ARE the content (the user's best practices), not a special protected case; empirical trivia ("Zod validates at runtime") and open questions do NOT belong in a skill (an open question is project content). Only residue: **`curate-skills` prunes what's WRONG or OUTDATED, never "it's just an opinion."** **Issue #5 RESOLVED — altitude routing.** Route each cross-cutting rule to the skill whose scope MATCHES the rule's scope (its "altitude"): a tool quirk → that tool's skill (supabase); a framework pattern → the framework skill (nestjs); a broad principle that transcends the tools ("never let the client touch the DB directly" — true for any DB/stack) → a high-level **concept skill** (e.g. `architecture`), NOT a tool skill. Genuine integration gotchas (exist only when two tools meet, e.g. Supabase pooler needing a specific Nest config) → the tool that's the SOURCE of the quirk, with a one-line pointer from the other; never duplicate. **NEVER combo / tool-tuple skills** (`nestjs+supabase`) — combinatorial explosion; concept skills absorb cross-tool themes instead. Rejected the parent-child framing (Supabase is NOT a child of NestJS — they're peers you combine). **Issue #4 (Audit) PARKED.** → **Operational-layer redesign thread FULLY CLOSED** (#1 reshaped into inbox model, #2 resolved via review-step trigger, #3 dissolved — no taxonomy, #4 parked, #5 resolved). THEN parked mechanical fixes (a roadmap→backlog, b temp→tmp) + earlier queue (grill-mode design, superpowers-flow marketplace round, battle-tests, harvest delapse/lumacraft/framework-build knowledge into skills). Nothing built yet — all catalog/template edits (capture-context routing migration, new `organize` + `curate-skills` skills) await their own approved plan.

---

## Latest — 2026-07-23 (session 14d): knowledge-ecosystem re-examination OPEN + grill-mode idea captured

- **Thread OPEN — re-examining `design-skill-ecosystem.md`'s locked branches, esp. #2 (`maintaining-skills`).** Trigger: brainstorm SKILL.md still references `docs/agents/conventions.md`/`workflow-rules.md` — files that never exist in Flow — which exposed that the knowledge layer is designed but entirely unbuilt, and the user states he was never fully confident in the locked design. Concerns on the table: (1) **ambient-skill overload** — `capture-context` is already always-on; adding another active skill during brainstorm/execute/research would wreck the agent's focus. Direction under discussion: at most ONE ambient behavior (capture-context's Notice lane, kept dumb — one line, no routing); `maintaining-skills` strictly invoke-only; stub-skill creation moved out of mid-work into the Sort pass; **the missing piece is a guaranteed trigger for the Sort pass** (nothing schedules it today — the loop silently not running is the old lose-conventions failure again). (2) **Opinionated knowledge** — much of the future harvest is stances (e.g. "client never talks to the DB directly"), not verifiable facts; janitor rule 4 (write-only-confirmed) doesn't fit stances; needs a fact/stance/open-question distinction, which also shields stances from rule 2's delete-when-unsure pruning. Nothing re-locked yet — discussion mid-flight.
- **NEW IDEA (captured, NOT designed — don't lose):** **red-team / grill mode.** An adversarial pass where the agent switches roles and attacks the current design/proposal — extremely judgmental, digging for flaws — as a safety step before a design locks. Ancestor: Delapse's `grill-me` / `grill-with-docs` (spec stress-test after brainstorming) — mine those when designing this. Placement TBD (brainstorm mode vs post-spec step).
- **Parked mechanical fixes — DONE (2026-07-24):** roadmap→backlog rename executed, all `temp/`→`tmp/` swaps done across flow + flow-skills + workbench CLAUDE.md + design-skill-ecosystem.md, workbench `temp/` dir physically renamed to `tmp/`. Historical `temp/` paths elsewhere in *this* session doc were left as-is (the dir is now `tmp/`; those are dated records). Original spec: (a) `flow/docs/work/roadmap.md` → `backlog.md` flat list; sweep `flow/README.md` + `now.md` (also fix stale "brainstorming" skill name there); extend capture-context's defer-to-existing line to route backlog items to `docs/work/backlog.md` when present. (b) `temp/` → `tmp/` everywhere: flow `.gitignore` + README; `research/SKILL.md` ×6 + `fetch-docs.sh` ×3; `debug-web-pages` youtube-watch.md + ROADMAP.md; workbench CLAUDE.md ×2 + physical `mv temp tmp` + live-pointer sweep in `new-workflow/`.
- **Stale-doc flag:** ecosystem Branch #4 says core "how to work" skills live inside the template — superseded by reality (all six live in the catalog, template ships zero). Needs a superseded marker when the doc is next touched.
- **Adversarial pass on the locked ecosystem design (agent's committed positions — PROPOSED, user has NOT reacted yet):**
  1. **One-ambient-behavior principle:** only capture-context may run ambiently; everything else invoke-only. Three violations in the locked design: (a) capture-context's shipped routing table forces a where-does-this-go decision mid-work — Notice must get dumber (one line, one fixed landing file, zero routing); (b) "create a stub skill on the spot" is a mid-work context switch — stub creation moves into the Sort pass; (c) Grow never runs interleaved with brainstorm/execute/research.
  2. **Biggest flaw — no Sort trigger.** "At a natural stopping point" never fires by itself; captured lines would rot in the landing file (the old lose-conventions failure, one file later). Candidate fix: mandatory wrap step in `execute` (milestone finish → review captures → Sort) + user-invocable janitor pass for cross-project cleanup. THE question to walk first.
  3. **Fact/stance/open-question split in knowledge entries.** Janitor rule 4 (write-only-confirmed) fits empirical findings, not stances (no-enum, client-never-talks-to-DB-directly). Stance = chosen position + one-line why; changes only when the user changes their mind; shielded from rule 2's delete-when-unsure. Opinionated is the VALUE of a personal KB (Audit checks conformance to the user's stances); stranger-facing labeling deferred with release discipline.
  4. **Audit must be scoped** to the skills the current work actually touched — unbounded "check against all accumulated best practices" is enormous reads.
  5. **Cross-cutting routing rule:** no perfect taxonomy; route to where you'd look it up first, never duplicate — slightly-wrong-but-consistent home beats agonizing.
  6. **Core verdict:** skill-as-knowledge-unit survives the attack (git-tracked, symlink-live, per-skill install, native on-demand loading); what fails is the operational layer (ambient/invoked boundary, missing trigger, fact/stance split, audit scoping) — that's the redesign scope.
- **RESUME POINTER (14d).** Thread mid-flight: user asked for compaction before reacting to the adversarial-pass positions above. Next: user reacts to positions 1–6 → then walk the redesign one question at a time, **starting with the Sort trigger (#2)**. After that thread closes: execute parked mechanical fixes (a)+(b) above; then the earlier queue (superpowers-flow marketplace round, battle-tests, Group D, harvest of delapse/lumacraft/framework-build knowledge into skills per the corrected model).

---

## Latest — 2026-07-23 (session 14c): Flow named, repos renamed, sweep done

- **Workflow named "Flow."** Short, relatable, accepted knowingly despite heavy overload (Meta Flow, Kotlin Flow, MS Flow etc.) — functionally fine as a plugin namespace. `rekindle` was clean but rejected as too long and unrelatable.
- **Plugin name = `flow`, not `flow-skills`.** The plugin name drives the autocomplete namespace (`flow:brainstorm`), independent of the repo name. Repo keeps the `-skills` suffix for clarity.
- **Naming map locked:**
  - Template repo: `flow` (GitHub `Adrian333Dev/flow`) — marked as GitHub Template Repository (Use this template button, creates repos with clean history)
  - Skills catalog repo: `flow-skills` (GitHub `Adrian333Dev/flow-skills`)
  - Plugin in `.claude-plugin/plugin.json`: `flow`
  - Skill invocations: unchanged plain verb-first names (`brainstorm`, `research`, …)
  - Install command: `npx skills add Adrian333Dev/flow-skills`
- **Rename sweep complete.** `plugin.json` name → `flow`; `flow-skills/README.md` (title + install URL + disclaimer); `flow-skills/CLAUDE.md` (install URL); `flow/scripts/check-skills.sh` (install URL); `flow/README.md` created; workbench CLAUDE.md layout table updated.
- **haiku-worker.md moved into catalog.** Relocated from template's `.claude/agents/` to `flow-skills/skills/execute/haiku-worker.md`. Agent-registration frontmatter stripped — it's a read-at-dispatch brief. Execute dispatch reads it at `~/.claude/skills/execute/haiku-worker.md` by path.
- **`scripts/` convention for catalog executables.** `debug-web-pages/tools/` → `scripts/`; `research/fetch-docs.sh` → `research/scripts/fetch-docs.sh`. Convention recorded in `flow-skills/CLAUDE.md`.
- **merge-files.js stays in the template** as a generic project utility, documented in template CLAUDE.md. Execute's multi-source brief section marked optional.
- **Symlinks fixed.** All 6 skill symlinks were broken by the local folder rename (`skills/` → `flow-skills/`). User ran `ln -sfn` loop to point them at the new `flow-skills/skills/...` path.
- **Next design round (deferred): superpowers-style plugin namespace.** Adding `marketplace.json` and switching to the plugin install door will physically relocate skills, breaking `~/.claude/skills/...` hardcoded paths in execute, research, debug-web-pages, and check-skills.sh detection. Needs its own design session.
- **RESUME POINTER.** Pending commits in both repos (`flow` and `flow-skills`). Next work: (a) superpowers-flow design round (marketplace.json + plugin-door install-path implications); (b) battle-test `explain` + reshaped `brainstorm` + new `research` in a real project brainstorm; (c) Group D general-ideation lens; (d) branch #5 init-integration PAUSED; (e) mine previous projects' rules files; (f) future `ui-mockups` skill.

---

## Latest — 2026-07-23 (session 14b): `research` EXPANDED, `executing-plans` → `execute` joins the catalog, chaining goes global

- **`research` rewritten — identity widened** from "research before recommending" to "never work against an external tool from training memory." Fires at recommendation, spec, plan (the sharpest case — a plan from memory bakes stale APIs into every task), and mid-execution. **Four-rung depth ladder:** targeted lookup (Context7 / one doc page) → working-against-a-tool (llms.txt route) → deep customization (shallow-clone the source into `temp/refs/<tool>/repo`, clone without asking, announce in turn) → landscape (external prompts, unchanged). **llms.txt route (live-verified this session):** both `llms.txt` (index; ai-sdk = 2 KB navigation map) and `llms-full.txt` (whole docs; ai-sdk = 5.5 MB → grep-corpus, NEVER read inline) fetch cleanly via curl — the browser lazy-load is rendering-only. WebFetch summarizes → cached verbatim files are the source for exact signatures. Bundled **`fetch-docs.sh`** chains candidate locations (domain root / `docs.` subdomain / `/docs` path), rejects HTML-error-page 200s, fetches BOTH variants, stamps source URL + date in `_sources.md`. Location patterns live INLINE in SKILL.md (no `sources.md` — rejected as unnecessary; no hardcoded per-tool URLs — rot). Fallback chain: Context7 → web search → ask the user; never silently fall back to training memory. **Delegation:** heavy reading (many pages, clones) goes to a cheaper-model subagent with a self-contained brief; main agent reads only the findings file.
- **Reference layout (user's shape — an over-engineered co-located `cache/`-subfolder variant was REJECTED):** raw refetchable material → `temp/refs/<tool>/` (existing temp-is-ignored convention — no new ignore machinery; persists across sessions/milestones, refresh when stale); committed curated references of any kind (e.g. an analyzed-codebase index à la lumacraft) → `docs/refs/`, flat files by default; `docs/notes/` unchanged for topical findings. Template **`.gitignore` created** (`temp/`) — it had none.
- **Chaining is now a GLOBAL rule.** Template CLAUDE.md's "Batch sequential checks" (build/test/lint only) rewritten to the principle: "if chaining won't bite back, chain — any operations, any phase"; separate calls only when output must be inspected between steps or a partial run is hard to detect/undo. The two chaining mentions in the execute skill stripped — skills say WHEN to verify, HOW lives once in CLAUDE.md.
- **`executing-plans` moved to the catalog and RENAMED `execute`** (user ran move + rename + symlink) — settles session-14's open question; it consumes the plan format `writing-plans.md` defines, so producer and consumer now live together. The template ships ZERO skills. `check-skills.sh` now guards all five required companions (brainstorm/research/explain/context-capture/execute). `writing-plans.md` refs updated to the new name; H1 retitled `# Execute`. Catalog = 6 skills; plugin.json + README + CHANGELOGs wired.
- **Naming convention LOCKED: verb-first** for skill folders and sub-files, recorded in `skills/CLAUDE.md` Conventions. Applied: `writing-plans.md` → **`write-plan.md`** (parallel to `write-spec.md`; refs swept in brainstorm SKILL.md + write-spec.md + CHANGELOG); `debug-web-pages` H1 title-cased. **`context-capture` → `capture-context` DONE** (user ran mv + symlink recreate; agent swept: frontmatter/H1/`/capture-context` invocation, brainstorm SKILL.md ×4, check-skills.sh, plugin.json, README, CHANGELOGs, template CLAUDE.md, recommended-tools.md).
- **RESUME POINTER (post-14b — supersedes session 14's below).** *User runs:* commit+push the **catalog** (research rewrite + fetch-docs.sh, execute move + de-chaining, writing-plans ref fixes, README/plugin.json/CHANGELOGs); commit the **v2 repo** (CLAUDE.md chaining rule, new .gitignore, check-skills.sh + execute, executing-plans folder removed). *Next work:* (a) **battle-test** `explain` + reshaped `brainstorm` + new `research` in a real brainstorm; (b) Group D general-ideation lens; (c) branch #5 init-integration PAUSED (owns the install-companion-skills setup step); (d) TODO mine previous projects' rules files; (e) future `ui-mockups` skill (not urgent).

---

## 2026-07-23 (session 14): skills went GLOBAL — brainstorm/research/context-capture moved to the catalog, writing-plans folded into brainstorm

- **`explain` finalized as a catalog skill.** User moved it to `skills/skills/explain/`; description genericized (no "replaces the old visualization skill" — consumers have no such context; no brainstorm-only framing); body de-workflow-ed ("mid-brainstorm" → "design discussions"). **Sequence-text ban REMOVED** (user reasoning: the old failure came from the old skill *recommending* that format; the new skill never lists it — don't write rules for problems that no longer exist; re-add only if it resurfaces in practice). Its useful tail survived as a positive rule: "**Dynamics go in prose.**" CHANGELOG.md added, README + plugin.json wired, global symlink live.
- **Global-skills shape LOCKED + EXECUTED.** `brainstorm` (renamed from `brainstorming`, per user), `research`, `context-capture` moved to the catalog (user ran the moves) and symlinked into `~/.claude/skills/`. **`writing-plans` folded into `brainstorm` as a second sub-file** (`writing-plans.md`, next to `write-spec.md`) — a plan is only ever written after brainstorm+spec, so it has no standalone trigger; this also killed the dangling `superpowers:writing-plans` handoff. **Dividing line: sub-file vs own skill = "can it fire on its own outside the flow?"** (research and explain do → own skills). `executing-plans` stays in the template for now; deciding whether it follows is open (it consumes the plan format writing-plans defines).
- **Distribution model:** template consumers install the companion set via `npx skills add Adrian333Dev/skills` (per-machine, not per-project). NO vendored copies in the template (drift — the debug-web-pages local-copy divergence is the precedent). **Enforcement = SessionStart hook** `scripts/check-skills.sh`, wired in `agentic-workflow_v2/.claude/settings.json`: silent when `brainstorm`/`research`/`explain`/`context-capture` are all present, otherwise injects "missing skills — do NOT improvise, tell the user to install". No CLAUDE.md setup section (user rejected: permanent dead weight after setup). The init flow (branch #5, still paused) will run the install as a setup step.
- **Group A remainder DONE (the fold-in, generic rules only):** `brainstorm` SKILL.md got the phased-output rules (orient and first branch question are separate messages; tree never printed; branches walked in plain free-form prose, no `Branch A:`/checkbox notation; lightweight tree-less conversational mode), locked-decisions-only batched writes, output-path project override, and a companion-skills/sub-files coordination section. `research` SKILL.md got fallback recording targets (no brainstorm/spec active → inline or `docs/notes/<topic>.md`) + the zero-lines-read presentation rule. `write-spec.md` handoff now points at `writing-plans.md`. User-personal rules (voice-to-text, /copy) stay in the template CLAUDE.md — never in catalog skills. CHANGELOGs + README entries + plugin.json done for all three.
- **RESUME POINTER (post-session-14 — supersedes session-13's below).** *User runs:* commit+push the **catalog** repo (5 skills), commit the **v2** repo (skills moved out, hook + check-skills.sh added). *Next work items:* (a) **battle-test `explain` + the reshaped `brainstorm`** in a real brainstorm; (b) Group D general-ideation lens; (c) branch #5 (init-integration) still PAUSED — now also owns the install-companion-skills setup step; (d) TODO mine previous projects' rules files; (e) future `ui-mockups` skill (not urgent); (f) decide later whether `executing-plans` moves to the catalog too.

---

## 2026-07-22 (session 13): rules hardened + first catalog skill wired + `explain` skill built (Group B DONE)

- **`/copy-full` tool superseded by a hard rule.** Adopted the read-aloud case2 CLAUDE.md phrasing into v2 `CLAUDE.md`: "all explanations go at the END of the turn, after every tool call" + re-deliver-in-full-if-violated. The parked tool (see session-12 note below) gets built only if the rule proves insufficient in practice.
- **Install rule broadened — NO exceptions.** Now "never run install or setup commands", explicitly including `pnpm add`/`remove`. The old "project deps the user asked for" exception is GONE, in both v2 `CLAUDE.md` and the workbench `CLAUDE.md`. Agent names the command; the user runs it.
- **Plan-first + no-AskUserQuestion migrated to the product** — user overrode session-12's "deliberately not migrated" call; both are now v2 `CLAUDE.md` hard rules (plan-first phrased to not conflict with locked-decision batch-writes). Workbench-only rules remaining: plain-conversational-only, reference/-read-only.
- **First catalog skill: `skills/skills/debug-web-pages/`** (user copied it in from the read-aloud project). Wired per the catalog's own CLAUDE.md: path added to `.claude-plugin/plugin.json` → `skills`, listed under `## Skills` in `README.md`. README now states the catalog is **written/tested for Claude Code only** (Codex, Cursor etc. unconsidered). Remote `Adrian333Dev/skills` already configured. **User publishes:** `git add` + commit + push.
- **Skill update/merge strategy (LOCKED session 13):** (a) *User's own machine:* ONE global symlink `~/.claude/skills/debug-web-pages` → the catalog clone's `skills/debug-web-pages` — edits from any project land in the clone's working tree, `git diff` = the merge review, commit+push publishes (matches ecosystem branch #4's "link, never hard-copy"). The read-aloud project still has a local COPY at `.claude/skills/debug-web-pages` — diff it against the catalog first (it may be newer), then replace with the symlink. (b) *Consumers seeing what changed:* **per-skill `CHANGELOG.md`** — dated entries, newest first, appended with every meaningful change. Chosen over agent-review / semantic-merge machinery (rejected as over-engineering). Convention added to `skills/CLAUDE.md`, pointer in README Layout, first changelog written for `debug-web-pages`.
- **TODO — mine previous projects' rules files:** collect hard-rules / conventions / CLAUDE.md files from all the user's past projects; harvest the good rules into `new-workflow/` (adopt or at least take inspiration). Not started — don't lose this.
- **Skills-catalog README rewritten (user disliked the wording):** internal design vocabulary ("what I know about tool X") removed, the not-the-workflow-template disclaimer cut (meaningless to strangers), the whole Layout section deleted (frontmatter note = common knowledge; flat-structure planning lives in `skills/CLAUDE.md`); the check-CHANGELOG-after-pulling pointer moved into Install where it's actionable. README = plain intro + Claude-Code-only blockquote + Install + Skills + one authoring-conventions pointer line.
- **RESUME POINTER (post-session-13).** *User runs (agent never does):* (1) publish catalog — `cd skills && git add -A && git commit -m "feat: add debug-web-pages skill" && git push -u origin main`; (2) global symlink `ln -s ~/code/projects/agentic-setup/skills/skills/debug-web-pages ~/.claude/skills/debug-web-pages`; (3) diff the read-aloud project's local copy at `.claude/skills/debug-web-pages` against the catalog (may be newer), fold in, replace with symlink; (4) commit the v2 repo — `cd agentic-workflow_v2 && git add -A && git commit -m "feat: replace visualization skill with explain"` (covers skill swap + ref sweep + "Who the user is" stub + earlier rule edits). *Next work items:* (a) **Group A remainder** — fold the comms rules + brainstorming phased-output rules (session-12 block below) into the `brainstorming` + `research` skill bodies; (b) **battle-test `explain`** in a real brainstorm (pattern library is unvalidated beyond the proposal shape); (c) Group D general-ideation lens; (d) branch #5 (init-integration) still PAUSED; (e) TODO mine previous projects' rules files (above); (f) future `ui-mockups` skill (not urgent).
- **Study-case corpus reorganized:** now `temp/study-cases/read-aloud-app/case1/` (full-architecture + brainstorm.md + chat-history.txt + the 2 SVGs) and `case2/` (that project's live CLAUDE.md, brainstorm.md, `visualizations/` incl. **`2026-07-22-explanation-study-case-report.md`** + who-plays-the-audio v1/v2/v3). **Group B escalated:** user verdict — viz + explanations were "so terrible" they get their own dedicated study-case session; case2's report is the primary evidence file.
- **Group B RUN (same session): explanation-skill rules designed + VALIDATED.** Study-case analysis of the four failed C5 attempts. Diagnosis (user-confirmed): the failures were never about calibration or unit size — the attempts oscillated on those two axes while the real missing dimensions were (1) whole-picture-first (narrowing to one isolated concept was the WRONG lesson from attempt 0 — user ranked the 7-diagram full-architecture file the BEST attempt, v1/v2/v3 "complete trash"), (2) priority ordering (lead with what the proposal IS + what the user must react to; mechanism trivia compressed/last), (3) gap discovery (agent guessed knowledge per attempt instead of knowing it). **The LOCKED rule set:** (a) *calibration two-tier* — technology knowledge calibrates against a **"Who the user is" section in the project CLAUDE.md** (ONE paragraph, written at project-init, rarely updated; agent asks once when it genuinely lacks info — no per-explanation questioning); the *solution's own components* are new by definition → ALWAYS defined from zero before appearing in any diagram, regardless of expertise; (b) *structure* — proposal in 2–3 sentences → define components → whole-picture diagram → the one load-bearing rule with depth → interactions as short prose steps → "what you're deciding"; (c) *diagrams* — structural ASCII only, spacious, one idea per diagram; **sequence-text `A → B: message` format BANNED** (user: "most useless diagram I have ever seen" — supersedes session-12's seed note recommending it for handshakes); **SVG dead** (~10 min + ~80k tokens per diagram vs the need for constant cheap invocation — case1's two SVGs are the evidence), **mermaid dead** (visually confusing to the user + needs a renderer); (d) *speed is a first-class constraint* — explanation fires constantly mid-brainstorm, ~1-minute budget, the plan-first step is a lightweight internal outline, never an artifact. **Validation: counter-example `new-workflow/2026-07-22-explanation-counter-example-c5.md`** — same C5 material redone under the rules — user verdict **"10 out of 10… everything worked so well… I fully approve"**. It is the gold sample for the new skill. **BUILT (same session): the `explain` skill** at `agentic-workflow_v2/.claude/skills/explain/` (name `explain` per user — not `explanation`); `visualization/` archived to `temp/archived-skills/`. First build relied on rules + the single gold sample — user rejected that ("very poor skill… relying on a single example… will fail terribly on other scenarios"). Rework (approved): SKILL.md = **invariants** (whole-picture-first, two-tier calibration, priority, internal plan, ~1-min budget, inline delivery) + **adaptive structure** (the six-part proposal shape is the *default*, never a template — structure follows the material; mechanism/comparison/walkthrough shapes differ) + pointer to `references/diagram-patterns.md` = pattern **vocabulary, not menu** (rules-for-every-diagram incl. five-second test + layered stack / pipeline / timeline lanes / aligned axes / tree / side-by-side + table-beats-diagram + the banned sequence block as anti-pattern; improvising better-fitting layouts explicitly encouraged). `example.md` demoted to one-instance-not-mold. Refs swept: brainstorming SKILL.md (description, coordination table, Phase-2 instruction), recommended-tools.md. **"Who the user is" stub added to v2 CLAUDE.md** (one-paragraph profile, written at init, rarely touched). **Single-file merge (user, same session):** the skill fires constantly and can't draw without the patterns, so a separate `references/` read every invocation is waste — SKILL.md now contains everything (invariants + adaptive structure + diagram rules + pattern vocabulary + a STRIPPED worked example); `references/diagram-patterns.md` and `example.md` DELETED (user-confirmed; full example survives at `new-workflow/2026-07-22-explanation-counter-example-c5.md`). Per user note, SKILL.md's worked-example section is explicitly labeled "stripped excerpts, NOT the full artifact" (heading + intro) so an agent never mistakes the excerpts for the complete 10/10 shape. **UI mockups → future separate skill** (user: not urgent, don't build now) — the explain skill's HTML ban stands; the session-12 HTML-escalation note transfers to that future skill's design. Pattern library is best-effort pending battle-testing (only the proposal shape is user-validated).

---

## Latest — 2026-07-21 (session 12): battle-test feedback — communication + viz + memory overhaul

Real battle-test of v2 (`brainstorming` + `context-capture` + `visualization` + `debug-web-pages`) on a **read-aloud-app** design surfaced concrete quality issues. This session pivoted from branch #5 to fixing them. **Mode: plain conversational brainstorming (no skill machinery).**

**Study-case artifacts** (`temp/study-cases/read-aloud-app/`) — inputs for the viz rebuild + the comms fixes:
- `2026-07-21-full-architecture.md` — the ASCII-viz reference **+ a Self-critique section** that seeds the new skill's patterns.
- `brainstorm.md` (63KB) — the session's output doc (good branch-tree/decision-capture quality; already notes "memory is retired").
- `chat-history.txt` (160KB) — full chat; **mine for concrete before/after examples when building Group A/B.**
- `reading-pipeline.svg`, `word-alignment.svg` — the overkill SVGs (~10 min to generate) that triggered the viz U-turn.

Decisions grouped A–D:

**Group A — communication rules.** **DONE (session 12): added to v2 `CLAUDE.md`** — new `Working with the user` + `Communication (/copy discipline)` sections, plus hard-rule bullets (no-auto-memory, no-installing-tools, destructive-delete-safety, no-internal-reasoning), plus the `Surface reasoning` rule reconciled with batch-writes. `hard-rules.md` surface-reasoning contradiction fixed + pointer to v2 CLAUDE.md added (no full duplication, to avoid drift). Also in the *workbench* `CLAUDE.md` and in effect now. **Migration correction (session 13):** the user overrode the earlier "not migrated" call — plan-first and no-AskUserQuestion ARE product rules too (now in v2 CLAUDE.md). Only plain-conversational-only and reference/-read-only stay workbench-only. **✅ DONE session 14 — folded into the `brainstorm` + `research` skill bodies (generic rules only; user-personal ones stay in CLAUDE.md).** The rules:
- **`/copy` constraint (root of the worst annoyance):** the user copies the LAST message via a `/copy` command. So put ALL tool calls (reads AND writes) BEFORE the final prose; the full response is the last thing in the turn. Never prose-then-edit. (An earlier "prose first, edits after" idea was WRONG — it breaks `/copy`.)
- **Batch writes; write only locked decisions.** In brainstorming, write to `brainstorm.md` only when a decision is genuinely locked (no open threads on it — not mid-discussion agreement); let several accumulate, record together. No yes/no gating questions — agent judges lock-state and writes without asking.
- **Explain artifacts from zero (narrowed):** never assume the user read a research report or file; research reports = assume ZERO lines read; explain what it says/concludes/proposes/why in plain language. Does NOT apply to earlier chat messages (the final-message-last rule means they were read).
- **Voice-to-text hard rule:** user almost always dictates → transcription errors (misspellings, wrong/dropped words, homophones). Read for intent, not literal wording.
- **Brainstorming voice:** free-form prose, NOT telegraphic; zero jargon/cheerleading/filler; never "you're absolutely right." (Telegraphic is fine in other contexts.)
- **Brainstorming phased-output rules (PRESERVED from now-deleted memory — fold into the `brainstorming` skill body):** (a) never print the full branch tree AND walk the first branch in the same message — Phase 1 (orient) and Phase 2 (walk branches) are separate messages, stop and wait between them; (b) never print the decision tree to the user *at all* — it lives in `brainstorm.md` only and is updated silently as branches resolve; walk a branch in plain language (situation → options → recommendation), no `Branch A:` / `[ ]` notation / tree references in the conversation; (c) lightweight conversational brainstorming skips the tree entirely — one question at a time.

**Group B — visualization U-TURN** (archive current, build new) — **SUPERSEDED by session 13's validated rule set (see Latest above); the seed notes below are historical (sequence-style `A → B` flow is now BANNED, not recommended):**
- New skill = **text formats (DEFAULT, usually best — keep the existing `text-formats.md` core) + ASCII diagrams (SECONDARY, only where a diagram beats text, and only with solid battle-tested patterns).** ASCII is inline (immediately visible) = also serves the "everything in the message" principle. SVG/HTML-render is too slow/expensive and over-complicates basic things.
- **UI mockups:** ASCII-first for simple/clean layouts; escalate to **HTML only when UI gets complex** (toolbars, tabs, dense components — ASCII carries only rough layout). Keep the HTML mockup path for that case.
- **ERD dropped entirely** (schemas explained in text/ASCII — no more DBML/Mermaid).
- **Archive** the current `visualization/` skill to `temp/archived-skills/` (do NOT delete).
- ASCII pattern seeds (from the study-case Self-critique): ASCII carries structure well, two-way/dynamic relationships poorly → use sequence-style text-flow (`A → B: msg`) for handshakes; cadence = short text (what it IS) → diagram → one-line bridge → next; top-down zoom + split large into small; plain labels only (no internal branch codes). **The architecture file is a SEED, not finished patterns** — the user says ASCII "didn't work so well" without standardized patterns, so text stays the safer default and ASCII needs a real good-vs-bad pattern library.

**Group C — removals (DONE except one confirm):**
- **Memory retired + DELETED.** `~/.claude/.../memory/` (13 files: `MEMORY.md` + 12) deleted in session 12 (user confirmed — "just delete it"). Load-bearing facts were already migrated to the *workbench* `CLAUDE.md`; the two brainstorm-format rules are preserved in the Group A block above for the brainstorming-skill edit. The product memory-off rule now lives in v2 `CLAUDE.md` hard rules. The memory feature is off for good — everything durable goes in the repo (this doc, CLAUDE.md, or a skill).
- **`research-evaluation` skill removed.** Archived to `temp/archived-skills/research-evaluation/`. Ranked external-LLM note (**Claude > ChatGPT > DeepSeek > Gemini**) added to v2 `recommended-tools.md` §4 (user-facing, for installers). **Do NOT put model-choice in v2 CLAUDE.md** — that idea was session-specific; it lives in recommended-tools.md. **User still needs to commit the two v2-repo changes** (recommended-tools.md edit + research-evaluation removal). Live v2 skills now: brainstorming, context-capture, executing-plans, research, visualization, writing-plans.

**Group D — scope reframe:** the workflow is now used for **general ideation** (product/project brainstorming from scratch), not just code-project bootstrapping. Apply as a LENS when finalizing CLAUDE.md + remaining skills (strip "we're in a codebase" assumptions); also feeds **branch #5** (`project-init` must handle "starting from just an idea").

**NEXT ORDER:** (1) ✅ memory deleted. (2) **Group A** — ✅ v2 `CLAUDE.md` + `hard-rules.md` done; **remaining: fold the comms rules into the `brainstorming` + `research` skill bodies** (incl. the phased-output brainstorm rules above). (3) **Group B** — archive current viz skill, build the new text+ASCII skill (use the study-case files + Self-critique). (4) **Group D** lens. **Branch #5 stays PAUSED** until the battle-test fixes land.

**Parked idea — custom `/copy-full` tool (feasible; deferred, not building now):** a small script that reads the session JSONL transcript (`~/.claude/projects/<slug>/<session>.jsonl`) and copies ALL assistant output since the last user message to the clipboard (`clip.exe` on WSL). Wire it to a **Stop hook** for auto-copy every turn, or run on demand. NOT achievable as a pure slash command — those inject a prompt into the model, they don't touch the OS clipboard. **Payoff:** if built, it makes `/copy` capture the whole turn regardless of edit placement, which REMOVES the need for the all-tool-calls-before-prose discipline. **Update (session 13): probably unnecessary** — the end-of-turn HARD RULE adopted from read-aloud case2 covers the problem behaviorally; build only if that rule keeps failing.

---

## Latest — 2026-07-19 (session 11): repo reorg + skills catalog scaffolded

**This `agentic-setup/` dir is now the CONTAINER**, not a workflow itself. Structure locked:

- `agentic-workflow_v2/` — the template (Repo A). **Renamed from `agentic-setup_v2/`** and `git init`'d (own `.git`) by the user this session.
- `skills/` — the personal skills catalog (Repo B), **newly scaffolded + `git init`'d** (own `.git`). Mirrors mattpocock's install-critical shape: `.claude-plugin/plugin.json` (name `adrian-skills`, empty `skills[]`), flat `skills/`, `README.md`, `CLAUDE.md` authoring guide, `.gitignore`. Installs via `npx skills add Adrian333Dev/skills` once pushed + a first `SKILL.md` exists. **Buckets deferred** (flat start, no `misc`); **versioning/changesets deferred** (branch #4). CLI finding: `npx skills init` is **per-skill only** (`<name>/SKILL.md`) — there is no repo-scaffold command; `add` works on any repo whose `SKILL.md` files carry `name`+`description` frontmatter.
- `new-workflow/` — design lab (design docs + `research-log/`), now clone-free.
- `reference/` — read-only: `v1-template/` (archived old root `README/SETUP/AGENTS/CLAUDE` + `docs/`), `framework-build/`, and the 7 external clones moved out of `new-workflow/` (mattpocock's → `reference/mattpocock-skills/`).
- New root `CLAUDE.md` = **container/workbench instructions** (NOT the template's shipped CLAUDE.md, which stays deferred until v2's skills are complete). Kept in place: `.claude/settings.json` + `settings.local.json`, `.codex/config.toml`, `.mcp.json` (context7 + playwright).

**Pending git (user runs — never the agent):** `git rm -r --cached agentic-workflow_v2 skills` so the parent stops tracking the two nested repos; gitignore the external clones under `reference/`; push `skills/` to `Adrian333Dev/skills`.

**Still open:** archive orphaned v1 `.claude/commands/*` + `.agents/skills/checkpoint/` into `reference/v1-template/` (flagged, awaiting go). **NEXT design action unchanged: branch #5 (init-integration seam).** First catalog skill still to be authored (via `npx skills init <name>`).

---

## What we're doing

Building a **new version of the agentic-setup workflow** — a curated plugin that combines and modifies skills from multiple sources. This is NOT another clean-framework-from-scratch effort. It's a practical, shippable combination of existing skill libraries, modified where needed.

**Why:** Building the custom framework from scratch is taking too long. This approach gets something working fast. Custom framework work continues separately after this unblocks the user.

---

## Sources being gathered

All cloned; **relocated to `reference/` on 2026-07-19** (superseding the `new-workflow/` paths below, and earlier `temp/repos/`). mattpocock's clone was renamed to `reference/mattpocock-skills/` so it doesn't clash with the new `skills/` catalog repo:

| Source | Origin repo | Location | Status |
|---|---|---|---|
| Superpowers (fork) | `Adrian333Dev/superpowers` | `reference/superpowers/` | Cloned, currently identical to upstream `obra/superpowers` v6.1.0. Being actively modified — see Pivot execution below. |
| agent-toolkit | `softaworks/agent-toolkit` | `reference/agent-toolkit/` | Cloned. Catalogued at `temp/refs/agent-toolkit.md`. Not yet curated. |
| mattpocock skills | `mattpocock/skills` | `reference/mattpocock-skills/` | Cloned. Categories: deprecated, engineering, in-progress, misc, personal, productivity. Not yet catalogued in detail. |
| taste-skill | `Leonxlnx/taste-skill` | `reference/taste-skill/` | Cloned. Reference notes at `temp/refs/taste-skill.md`. Not yet tried on a real task. |
| ui-ux-pro-max-skill | `nextlevelbuilder/ui-ux-pro-max-skill` | `reference/ui-ux-pro-max-skill/` | Cloned. "161 reasoning rules, 67 UI styles" per its README. Not yet evaluated in depth. |

---

## Open questions (answer these before brainstorming)

1. ~~**mattpocock skills**~~ — Resolved: `mattpocock/skills`, cloned at `new-workflow/skills/`.

2. ~~**ui-ux-pro-max-skill**~~ — Resolved: `nextlevelbuilder/ui-ux-pro-max-skill`, cloned at `new-workflow/ui-ux-pro-max-skill/`.

3. **Plugin location** — Where does the new plugin live? Options:
   - New standalone repo (e.g., `~/code/projects/my-plugin/`)
   - A folder inside `agentic-setup/`
   - Somewhere else?

4. **Installation target** — Does this replace Superpowers entirely (user removes Superpowers, installs new plugin), or sit alongside it?

5. **Any other skill sources** to gather before we start planning?

---

## Known problems to solve in the new plugin

### From Superpowers (must fix)

- **Brainstorming HARD-GATE**: Forces a 9-step mandatory process (context explore → questions → 2-3 approaches → design → write design doc → commit → self-review → user review → transition to writing-plans). Every step mandatory even for tiny tasks. → Needs to become lightweight, conversational, no forced doc writing, no commit step.
- **using-superpowers "1% chance" rule**: Causes constant over-invocation of skills. → Soften to "if it clearly applies."
- **Subagent-driven-development**: Was prohibited on Delapse; Superpowers kept recommending it. → Neuter or remove, OR replace with a smarter version.
- **Git mutations in skill steps**: Brainstorming step 6 says "commit". Must be removed everywhere.
- **AskUserQuestion tool**: Must be prohibited globally.
- **Post-compaction reset**: Skills re-applied as if overrides didn't exist after compaction. → Fix in CLAUDE.md so hard rules survive.
- **Context explosion**: ~90-100K tokens consumed at session start. → **Root-caused 2026-07-01, see "Pivot execution" below.** It is NOT the superpowers `hooks/session-start` hook (only ~860 tokens, fires on start/compact only — confirmed negligible). It IS root `CLAUDE.md`'s mandatory chain of 6 full skill bodies (~13,250 tokens) plus mandatory doc reads, forced every milestone. Fix is loosening `CLAUDE.md`'s workflow rigidity, not hook edits.

### New skills needed (not in Superpowers)

- **db-design guide**: Brainstorm complete (see `temp/refs/db-design-skill/db-design-guide-notes.md`). Base: agent-toolkit Skill-4, modified. Ready to write any session.
- **debug guide**: Core principle — agent must not state a cause without proof. "Hypothesis: X might cause this → verify by doing Y." Must replace the assumption-heavy debugging behavior.
- **backend-design guide** (name TBD): Domain/service organization, route hierarchy, API naming. Covers Delapse Issues 4, 6, 7. Needs its own brainstorm session.
- **brainstorm guide**: Draft exists at `framework-build/docs/guides/core/brainstorm.md` — treat as scratch only. Needs full redesign session.
- **execute/handoff/verify guides**: Still pending.

---

## Superpowers skills inventory

| Skill | Keep / Modify / Remove | Git-mutation audit (2026-07-01) |
|---|---|---|
| `brainstorming` | **Replace wholesale** (decided 2026-07-01 session 2) — not a line-edit. New mechanic = framework-build's branch-tree design (`framework-build/docs/design-session.md` D27-29, D45-46): Phase 1 Open → Phase 2 Walk (one branch at a time, agent commits to a recommended answer, user reacts) → Phase 3 Assumption-check (now a global cross-cutting rule — see Skill-by-skill audit section below, NOT brainstorming-specific) → Phase 4 Close. `brainstorm.md` = living working-memory doc; exact location TBD (depends on new-workflow's eventual milestone/session structure — still open). HARD-GATE removed → soft norm only ("don't start implementing before the design is roughly agreed"). Visual companion section removed → replaced by the merged `visualization` skill (see below). Spec-writing stays a separate, real, mandatory step for implementation work (brainstorm → spec → plan → execute, always, not optional/foldable) — needs its own `write-spec` skill mirroring framework-build's design (reads brainstorm.md + conversation, synthesizes spec.md, no interview). **Finalize LAST**, after write-spec, context-capture, assumption-check, and visualization are all designed — brainstorming references all of them. | Strip "write doc + commit to git" step (superseded by the wholesale-replace decision above) |
| `writing-plans` | Keep, modify | Strip literal "Step 5: Commit" (`git add`+`git commit`) baked into generated plan templates |
| `executing-plans` | Keep or modify — needs separate brainstorm to assess | No mutation — only references `using-git-worktrees` (a dependency reference, not a mutation itself) |
| `systematic-debugging` | Modify — add proof-required rule | No mutation — only read-only `git diff`/recent-commits mention |
| `verification-before-completion` | Likely keep — assess | No mutation — descriptive text only |
| `requesting-code-review` | Likely keep — assess | No mutation — read-only `git rev-parse`/`git log` for SHA lookups |
| `finishing-a-development-branch` | **Deleted (session 5).** Entirely git/worktree operations; only non-git piece (run tests before finishing) is covered by `verification-before-completion`. | Real offender — deleted |
| `subagent-driven-development` | **Delete entirely.** (User: "really annoying.") | Whole skill being removed |
| `test-driven-development` | Remove (D3: no TDD) | No mutation — descriptive only |
| `using-git-worktrees` | **Delete entirely.** Confirmed: don't need anything about git for this. | Whole skill being removed — its native-tool-fallback path ran real `git worktree add`/`git branch` mutations |
| `using-superpowers` | **Confirmed 2026-07-01 (session 2): Modify.** Soften/remove lines 10-16 (`<EXTREMELY-IMPORTANT>` "1% chance...ABSOLUTELY MUST...not negotiable" block) → replace with "invoke when it clearly applies." Also drop the Red Flags table and the forced-invocation `digraph` flowchart further down the file — same over-invocation doctrine reinforced twice more. Keep: Instruction Priority section, How to Access Skills section — mechanical/harmless, no change needed. | No mutation — one descriptive example string only |
| `writing-skills` | Keep for reference | No mutation — one optional checklist item ("commit skill to git"), low risk |
| `receiving-code-review` | Likely keep — clean read, no conflicts with our rules (verified 2026-07-01) | No mutation |
| `dispatching-parallel-agents` | Likely keep — clean read, no conflicts (verified 2026-07-01) | No mutation |

---

## agent-toolkit skills to include

High priority (from `temp/refs/agent-toolkit.md`):
- `session-handoff` — overlaps with our handoff.md need
- `gepetto` — multi-step planning reference
- `game-changing-features` — strategic brainstorming
- `requirements-clarity` — requirements gathering
- `reducing-entropy` — code quality

DB + stack:
- `database-schema-designer` — already analyzed, basis for db-design guide

Visualization (evaluate):
- `excalidraw`, `draw-io`, `c4-architecture`, `mermaid-diagrams`

---

## taste-skill / ui-ux-pro-max-skill

- **taste-skill**: Anti-slop frontend skill. GSAP dependency may conflict with shadcn/Tailwind stack. Try on a real UI task before committing. See `temp/refs/taste-skill.md`.
- **ui-ux-pro-max-skill**: Unknown — need to see it first.

---

## Pivot execution (2026-07-01)

Started executing the fork-fix list above, working directly in `new-workflow/superpowers/`. Mode for this whole effort: **plain conversational brainstorming only** — do NOT invoke the actual `superpowers:brainstorming` skill machinery (its HARD-GATE, forced spec doc, forced git commit, forced transition to `writing-plans`) for this meta-work. Ironic but deliberate: we're replacing that machinery, not running it on itself.

### Research findings (skill loading & token cost — use these, don't re-derive)

- **Skill loading is lazy everywhere**: only frontmatter (name + one-line description) loads automatically at session start, for plugin/project/user skills alike. Full `SKILL.md` body loads only on invocation. Confirmed via official docs (code.claude.com/docs).
- **No per-tool-call compounding.** Verified empirically by parsing a live session's own transcript JSONL: the full skill-catalog listing and the superpowers `using-superpowers` hook injection only re-fire on *discontinuity events* — session start, `/compact` (manual or auto), model switch (`/model`) — never on a per-tool-call basis.
- **The superpowers `hooks/session-start` hook is a red herring for token cost.** It's a plugin-level hook (not a skill — lives at `new-workflow/superpowers/hooks/hooks.json` + `hooks/session-start`, separate from `skills/`), fires on `matcher: "startup|clear|compact"`, and re-injects the full `skills/using-superpowers/SKILL.md` body as `<EXTREMELY_IMPORTANT>` context. But that file is only 62 lines / ~860 tokens — negligible even fired repeatedly. **Decision: leave the hook mechanism alone, not worth editing for cost reasons.** The only real problem in that file is the behavioral language at lines 10-16 (the "1% chance → must invoke" / "not negotiable" block) — already captured in the inventory table above.
- **The real source of large (50k+) token burns: the mandatory skill-body cascade in the current root `CLAUDE.md` workflow table**, not any hook. Measured directly: the standard milestone chain (`brainstorming → writing-plans → subagent-driven-development → verification-before-completion → requesting-code-review → finishing-a-development-branch`) loads **~13,250 tokens of skill bodies alone** every time it runs in full (subagent-driven-development ~5,400 tok, brainstorming ~2,600 tok, writing-plans ~1,770 tok, finishing-a-development-branch ~1,700 tok, verification-before-completion ~1,050 tok, requesting-code-review ~700 tok). On top of that: mandatory session-start reads (`now.md`+`workflow-rules.md`+`conventions.md`+`commands.md`), plus the active milestone's `spec.md`/`plan.md`/`session.md` (substantial in a real project), plus brainstorming's own "explore project context" reads.
- **Architecture implication**: don't build per-milestone dynamic skill-loading/toggling — the problem it would solve (context cost scaling with installed skill count) is smaller and differently-shaped than feared, since frontmatter cost is negligible and reinjection is event-bounded, not call-bounded. The actual lever is (a) loosen root `CLAUDE.md` so it doesn't mandate a long fixed read-then-invoke-6-skills chain, and (b) keep total curated skill count reasonable across whatever plugins get merged in.

### Root `CLAUDE.md` direction (decided, not yet executed)

Loosen the rigid milestone-flow backbone. Superpowers (the fork) becomes **one plugin among several, invoked when needed** — not the mandatory spine of every session. Stop mandating the fixed read-then-chain-6-skills sequence at session start; make the milestone structure lighter/opt-in.

### Next actions list (2026-07-01, original) — PARTIALLY SUPERSEDED, see below

1. Delete `new-workflow/superpowers/skills/subagent-driven-development/` entirely. — still pending
2. Delete `new-workflow/superpowers/skills/using-git-worktrees/` entirely. — still pending
3. Decide delete-vs-strip on `finishing-a-development-branch`, then execute. — still pending
4. Strip `writing-plans`' Step 5 commit block. — still pending
5. ~~Strip `brainstorming`'s commit-to-git step.~~ — superseded: brainstorming is being replaced wholesale, not line-edited (see below)
6. Check for + clean up dangling references to deleted skills in the remaining ones (e.g. `executing-plans/SKILL.md:68` and `subagent-driven-development/SKILL.md:409` reference `using-git-worktrees`; moot for the file being deleted, but check survivors). — still pending
7. Then: rewrite root `CLAUDE.md` per the "Root CLAUDE.md direction" above. — still pending

---

## Skill-by-skill audit + architecture decisions (2026-07-01, session 2)

Mid-session-1 we paused the mechanical git-mutation-strip walk to go skill-by-skill, file-by-file through the whole fork before touching anything, per explicit request ("I don't want you to immediately jump into action... go skill by skill, file by file, plugin by plugin"). This surfaced bigger architecture decisions beyond simple line-edits. Recorded here in full before compaction.

### Audit walk so far (in bootstrap order)

1. **`using-superpowers`** — confirmed Modify (see inventory table above). Locked.
2. **`brainstorming`** — confirmed Replace wholesale (see inventory table above). Locked at the architecture level; exact file content NOT yet drafted. Depends on write-spec, context-capture, assumption-check, and visualization all being designed first — finalize last.
3. Skills 3-14 (writing-plans, executing-plans, finishing-a-development-branch, subagent-driven-development, test-driven-development, using-git-worktrees, systematic-debugging, verification-before-completion, requesting-code-review, receiving-code-review, dispatching-parallel-agents, writing-skills) — **paused, not yet walked individually this session.** `receiving-code-review` and `dispatching-parallel-agents` got a clean read earlier (both fine, no changes needed — see inventory table). The rest still have only the original git-mutation-audit pass from session 1 (see inventory table) — need the same close read the first two skills got.

### New requirement: publish-ready changelog

User is considering publishing this fork publicly. Every deviation from upstream Superpowers needs to be documented with rationale as we go, not reconstructed at the end. **Decision: `new-workflow/superpowers/CHANGELOG.md`**, living in the fork repo root (OSS-idiomatic, travels with it if published). **Not yet created** — needs to be started and backfilled with the `using-superpowers` decision once we do the actual file edit (we've only agreed scope so far, not written the edit).

### Assumption-check — global hard rule, not a brainstorming phase

Corrected understanding (was wrong earlier in session): this is NOT specific to brainstorming. It's a hard rule that applies everywhere — anywhere the agent is operating on an unverified premise. The actual mechanism (how to detect/surface assumptions consistently across all phases, how many to surface, when it's noise vs. signal) is a genuinely hard problem. **Needs its own dedicated brainstorming session — not designed yet, do not improvise a mechanism.** Listed as a sub-skill to design before finalizing `brainstorming`.

### Context-capture — new cross-cutting skill, NOT yet built

Problem statement (from user): `brainstorm.md` solves note-capture during brainstorming, but spec-writing/planning/execution/research/debugging phases also surface things worth keeping — and currently there's no consistent mechanism for that. Some notes are milestone/task-specific; some are global/project-wide (e.g. a future-feature idea that surfaces mid-brainstorm on something unrelated) and need to land somewhere different so they aren't lost or miscategorized.

**This is already solved in framework-build's locked decisions** — `framework-build/docs/design-session.md` D30 + D31:
- **D30 — three-layer capture:**
  - Layer 1, phase-specific working memory: `brainstorm.md` during brainstorming, code-state notes during execution, findings during research.
  - Layer 2, durable knowledge, written **immediately** when it surfaces: domain/stack gotchas → `docs/kb/<domain>/<topic>.md`; global future ideas → `backlog.md`; project-level architectural calls → `decisions.md`.
  - Layer 3, session checkpoint: `session.md` at natural breakpoints and session end.
- **D31 — immediate capture rule:** write the moment something surfaces, not at session end (session end is too late — compaction or abrupt end loses the intent to save). Brief inline note, then keep working. User reviews/edits later.

This maps directly onto the "global vs. milestone-specific" split the user described — Layer 2 (KB/backlog/decisions.md) is the global side, Layer 1 (brainstorm.md/session.md) is the milestone-specific side.

**Recommendation (stated, not yet locked):** build this as its own standalone, cross-cutting skill in new-workflow — tentatively named `context-capture` — usable from any phase, not folded into brainstorming. **Not yet designed or built.** Listed as a sub-skill to design before finalizing `brainstorming`.

### Scope reframe: new-workflow ≈ framework-build v2

User clarified (see updated scope note at top of this file): this isn't strictly "fork Superpowers, keep framework-build separately paused." It's the same underlying effort, continued with a different method — adapting/curating existing Claude/Codex-compatible skill ecosystems instead of writing a guide-system from scratch. framework-build itself may be entirely rewritten or dropped. Its 85 locked decisions are a validated reference to pull from freely. New skills designed in new-workflow (context-capture, assumption-check) may get ported back into framework-build later if it resumes. Still: only *write* to `new-workflow/` files unless told otherwise.

### Visualization — merge `diagrams` (new) + `visualization` (old) into ONE skill

New skill discovered: `new-workflow/new-skills/diagrams/SKILL.md` + `references/advanced-patterns.md` + `references/complex-diagrams.md` + `examples/` (7 files — see below). Old skill: `framework-build/docs/guides/core/visualization/GUIDE.md` (+ `html-sketch.html` template, not yet re-read this session but referenced in design-session.md D81-85).

**Decision: ONE merged `visualization` skill, not two separate/gated skills.** Multiple format options living inside it (text formats, SVG diagrams, HTML UI mockups) that the agent picks between per situation — corrected from an earlier wrong framing (this agent initially proposed a rigid two-tier gate between them, which the user explicitly rejected: "we are definitely going to merge the visualization and new diagram skill into a single visualization skill").

**What the new `diagrams` skill provides (high quality, verified by reading SKILL.md + both reference files in full):**
- Self-contained HTML/SVG output, no external deps, opens in any browser, light/dark via CSS custom-property tokens.
- Box-width formula from text length, computed viewBox (not guessed), hard spacing/arrow-routing rules.
- Intent-based decision tree: routes on the verb the user used ("what's the flow" → flowchart, "what's the architecture" → structural/containment, "compare X vs Y" → side-by-side comparison columns, "how does X work" → illustrative/spatial-metaphor diagram).
- Progressive disclosure: `advanced-patterns.md` (comparison columns, status-coded success/fail paths, return/loop arrows, callouts, legends) and `complex-diagrams.md` (12+ node diagrams, lane/backbone planning, multi-way branch/merge, parallel background branches, self-loops, when to split into overview + detail diagrams) only get read when the diagram actually needs that complexity — good token economy, same principle as everything else in this effort.
- Judged: effectively a strict upgrade over the old guide's HTML-sketch section, which had layout principles but no concrete geometry/formulas.

**Corrections made by user to this agent's first-pass assessment (important, don't repeat these mistakes):**
1. **Not two gated tiers — one skill, multiple options.** (See above.)
2. **UI mockups:** user believes the new `diagrams` skill can likely already handle UI-component-state comparisons fine based on their own experience (not actually a gap requiring a separate tool) — but keep the old `html-sketch.html`/GUIDE.md approach available as a fallback option inside the merged skill for safety, rather than assuming full replacement untested.
3. **Mermaid policy:** old guide bans Mermaid outright. Reasons (from user, corrected from this agent's guess): (a) Mermaid needs a separate markdown-rendering step — friction; (b) Mermaid output is frequently confusing/unclear compared to the new skill's flows — NOT a claim about SVG having problems. The `diagrams/SKILL.md` decision tree (line 97) does carve out one exception — "draw the schema/ERD → use Mermaid.js via CDN, not hand-drawn SVG, table layout by hand fails every time" — that line is in the actual file, not this agent's invention, but the user is skeptical of it (doesn't recall SVG issues, the skill already leans on SVG successfully elsewhere). **Leaning: drop the Mermaid exception entirely, pure SVG/HTML for everything including ERDs** — the user's two stated objections to Mermaid apply equally to the ERD case. Revisit only if a real case proves SVG genuinely fails for ERDs specifically. **Not finalized — confirm before editing.**
   **Update (session 3, battle-test) — the case predicted here actually happened, see "ERD recipe — battle-tested and revised" below: dropping the exception was wrong, but Mermaid wasn't the final answer either. Landed on DBML + user's own `dbdiagram` VS Code extension.**
4. **Size/complexity constraints need review.** The `diagrams` skill was originally built for Claude.ai's web inline-diagram tool, which renders in a small embedded frame — hence the fixed `viewBox="0 0 680 H"`, "max 4-5 boxes per linear flow," "past 25-30 nodes, split into overview + detail diagrams," etc. In new-workflow's actual use case (a standalone HTML file opened in a real browser), there's no small-frame constraint — diagrams can be much larger and more flexible. **Action: review and likely loosen these size/complexity rules** before finalizing the merged skill — not yet done.
5. **Examples set needs review — not yet done by this agent.** User flags:
   - `examples/complex-system-example.html` is currently the file referenced as "the worked example" in `complex-diagrams.md`, but per the user it is **not** actually the most complex/best example available.
   - `examples/youtube_viral_loop_options.svg` is the most complex example AND the most visually clear — should likely become the primary referenced advanced worked example instead (or in addition).
   - `examples/outreach_agent_architecture.svg` — flagged as important to keep: good example of nested/wrapped containment across different environments ("wrapping notes inside different notes").
   - `examples/reply_relevance_scoring_fan.svg` — flagged as important to keep: another complex, unique example.
   - User suspects there may be too many examples overall (7 total: comparison-example.html, complex-system-example.html, emoji_mosaic_rendering_pipeline.svg, funding_credit_stack_flow.svg, outreach_agent_architecture.svg, reply_relevance_scoring_fan.svg, youtube_viral_loop_options.svg) and some could be trimmed/merged — but the three named above are explicitly worth keeping/featuring.
   - **Next action on resume: actually open and read `youtube_viral_loop_options.svg`, `outreach_agent_architecture.svg`, `reply_relevance_scoring_fan.svg`, and `complex-system-example.html`, assess them, decide example-set curation, and decide whether `complex-diagrams.md`'s "worked example" reference should change.** Not done yet — deferred to conserve context right before this compaction.

### Visualization — merge complete (2026-07-01, session 3)

All open items resolved and executed:

- **Examples curated**: read all 7 example files, dropped 3 redundant ones (`comparison-example.html` — subsumed by `youtube_viral_loop_options.svg`, which does the same comparison+status-coded+recommendation pattern with real content and a mask technique the synthetic one lacks; `funding_credit_stack_flow.svg` and `emoji_mosaic_rendering_pipeline.svg` — both taught nothing `complex-system-example.html` doesn't already cover). User deleted the three files themselves. 4 remain: `complex-system-example.html` (stays as `complex-diagrams.md`'s worked example, unchanged), `youtube_viral_loop_options.svg`, `outreach_agent_architecture.svg`, `reply_relevance_scoring_fan.svg`.
- **Mermaid/ERD exception dropped**: removed the one "use Mermaid for ERDs" carve-out from the decision tree. Replaced with a real `Recipe: entity-relationship (schema)` section in `SKILL.md` — table boxes (header + column rows, `PK`/`FK` tags) connected by cardinality-labeled lines instead of hand-drawn crow's-foot notation. Pure SVG now, no exceptions.
- **Size/frame constraints loosened**: root cause was `viewBox="0 0 680 H"` — width was hardcoded, tuned for Claude.ai's small embedded diagram panel; only height was ever computed. Fixed to `viewBox="0 0 W H"`, both computed from content, mirroring the pattern the skill already used for height. Cascaded through 4 other spots that justified their limits by citing the 680 number (spacing arithmetic, structural-nesting depth, comparison-column x-ranges, complex-diagram lane assignments) — all reworded to be content-driven, not frame-driven. Left the genuinely legibility-based rules alone (max 4-5 boxes/flow, 12-20/25-30 node budget, box typography) — those aren't frame artifacts.
- **Merged into one skill**, structure confirmed by user before writing: `new-workflow/new-skills/diagrams/` renamed to `new-workflow/new-skills/visualization/`. `SKILL.md` gained a **Step 0** (does this need a visual at all — ported from old GUIDE.md's escalation-gate + working-process rules) ahead of the existing decision tree, which became **Step 1** and gained a new top-level **UI mockup** row (routes to `references/ui-mockups.md`, HTML/CSS not SVG — applies to any UI markup request, not just comparisons, per user correction). Two new reference files ported from `framework-build/docs/guides/core/visualization/GUIDE.md`: `references/text-formats.md` (text-tree/numbered-prose/sequential-flow, No-Mermaid rule, telegraph style) and `references/ui-mockups.md` (layout principles, one-shot rules, explicitly marked as "default today, not a permanent lock" per user's note that the diagram machinery may eventually absorb mockup generation too). `advanced-patterns.md` and `complex-diagrams.md` carried over unchanged.
- **Palette unified**: user corrected an earlier "battle-tested, keep separate" framing from this agent — old HTML-mockup palette wasn't actually battle-tested, just visually spot-checked. Decision: **diagram system's CSS-custom-property tokens win everywhere; whenever old-mockup and new-diagram conflict, new-diagram wins** — because it empirically produces better results. `examples/html-sketch.html` copied over from `framework-build` and repaletted onto the diagram system's exact tokens (dropped `--text-faint`/`--border-inner`, which the diagram system doesn't have, rather than inventing replacements — collapsed into `--text-muted`/`--border`). Kept the manual light/dark toggle mechanism (JS class-swap) since it's an orthogonal UX affordance, not a color-system conflict — just wired to the same hex values as the diagram system's `prefers-color-scheme: dark` block.

Final file tree:
```
new-workflow/new-skills/visualization/
  SKILL.md
  references/{text-formats,ui-mockups,advanced-patterns,complex-diagrams}.md
  examples/{complex-system-example.html, html-sketch.html, outreach_agent_architecture.svg, reply_relevance_scoring_fan.svg, youtube_viral_loop_options.svg}
```

`framework-build/` source files were read, not edited — consistent with the read-only rule.

### ERD recipe — battle-tested and revised (2026-07-01, session 3 continued)

Battle-tested the merged `visualization` skill by actually generating and rendering diagrams (CI/CD flowchart, ERD schema), not just reading the skill. Flowchart recipe held up under real stress (17-node diagram, one trivial fix). ERD recipe did not — this is the case point 3 above said to watch for.

- **Round 1:** hand-rolled SVG table-boxes-and-lines recipe (the one written when the Mermaid exception was dropped) produced a schema diagram with no arrowheads on relationship lines, lines running straight through column text, and a line clipping back through its own source table's interior. Confirmed by screenshot, not just code review.
- **Round 2:** put the Mermaid exception back (`erDiagram`, cardinality tokens, save as `.md` fenced block). Structurally correct — real crow's-foot notation, no crossing lines, auto-routed curves — but visually plain/generic. User independently confirmed this with a separate Claude session producing similarly mediocre Mermaid ERD output.
- **Round 3 (settled, and written into `SKILL.md`):** schema/ERD diagrams use **DBML** as the artifact, rendered by the user's own **`dbdiagram` VS Code extension** (already installed — real-time local preview, no login needed for basic rendering, no copy-paste to a website). User confirmed the rendered output was good. `SKILL.md`'s "Recipe: entity-relationship (schema)" section, its Step 1 table row, the frontmatter description, and the "Output workflow (SVG diagrams)" line all updated to say DBML instead of Mermaid/SVG. `text-formats.md`'s "No Mermaid" footnote simplified back to a flat statement since there's no Mermaid usage left anywhere in this skill to carve an exception for.
- Test artifact: `temp/viz-test-4-erd-schema.dbml` (5-table schema: organizations/projects/users/tasks/comments — same one used for all three rounds).

**Two new hard rules from this same session (apply everywhere, not just visualization):**
1. **Never write scratch/temp files to root `/tmp`.** Use an in-repo temp location the user can actually browse (this repo's existing `temp/` at repo root, or a folder inside whatever `new-workflow/` area is relevant) — root `/tmp` paths are deeply nested per-session sandbox dirs the user can't easily find or read, and that's a real annoyance, not a style preference.
2. **Never install extensions/tools on the user's behalf.** If a VS Code extension or similar is needed, tell the user what's needed and let them install it (they frequently already have it installed).

### SKILL.md split into reference files (2026-07-01, session 3 continued)

User flagged that `SKILL.md` had grown into both a dispatcher and a reference manual (237 lines) — asked whether things like the base stylesheet belonged in the main file given text-formats will be the most common path and everything else is secondary. Agreed and split:
- `references/svg-diagrams.md` (new) — base stylesheet, arrow marker, all hard rules, box anatomy, the flowchart/structural/illustrative recipes, "Scripting large diagrams," and an SVG-specific done-checklist. Everything shared by the 3 SVG-based recipes, none of it needed for Step 0/Step 1 routing.
- `references/entity-relationship.md` (new) — the DBML recipe, moved out wholesale. It never touched the SVG stylesheet/hard-rules machinery anyway, and this now matches how `ui-mockups.md` already handles its own non-SVG recipe.
- `SKILL.md` — cut to 60 lines: frontmatter, Step 0, Step 1, a short "Output workflow" section that just points at the reference files, and a short generic checklist (detailed SVG checks moved into `svg-diagrams.md`).
- Fixed stale cross-references that pointed at the old inline location: `references/ui-mockups.md`, `references/complex-diagrams.md`, `examples/html-sketch.html`.

### Battle-test round 2 — comparison, UI mockup, illustrative (2026-07-02)

Finished the battle-test list left pending after the ERD/SKILL.md-split work: generated and rendered real diagrams for the three remaining families (flowchart and ERD were already covered in round 1). All test artifacts live in `temp/viz-test-5/6/7-*` (continuing the numbering from round 1's `viz-test-1..4`), not the repo root or the tool's `/tmp` sandbox.

- **Comparison** (`temp/viz-test-5-comparison-grill-skills.html`) — grill-me vs grill-with-docs, real project content. 2 columns, one asymmetric dead-end branch (coral, "not ready yet → use grill-me instead"), two teal success terminals, one insight callout, one legend. Rendered clean on first pass — no fixes needed.
- **UI mockup** (`temp/viz-test-6-milestone-dashboard-mockup.html`) — a milestone-status card in 3 states (idle / in-progress / ready-to-ship), adapted from `examples/html-sketch.html`. Verified in both light and dark via the built-in toggle. Clean on first pass.
- **Illustrative** (`temp/viz-test-7-progressive-disclosure-illustrative.html`) — spatial metaphor for the skill's own progressive-disclosure design: SKILL.md as an iceberg tip (always read, warm/amber), reference files as submerged blobs (cool/gray = dormant), one lit up amber + connected by a light-beam for the scenario "user asks to draw the schema" → `entity-relationship.md`. Uses the one permitted `linearGradient` for water depth. Clean on first pass.

**One real bug found and fixed:** `references/svg-diagrams.md`'s hard rules never said to add `width="100%"` to the `<svg>` tag — only `viewBox`. Without it, a browser renders the SVG at its default 300×150 intrinsic size and `viewBox` only rescales content inside that tiny box, so the diagram looks broken/invisible despite every coordinate being correct. The existing canonical example (`complex-system-example.html`) happened to do this right, but nothing in the recipe told a fresh reader to. Both new SVG test files were written without it initially, caught only because this agent happened to recall the pattern from the example file before rendering — a first-time reader following `svg-diagrams.md` literally would not know to. **Fixed:** added an explicit rule right after the ViewBox rule in `svg-diagrams.md`.

Two softer, unresolved observations (not fixed, flagged for later):
1. `advanced-patterns.md`'s comparison-badge guidance (red=breaks/green=works/gray-blue=neutral) doesn't cover the case where neither option is objectively better (situational choice, like grill-me vs grill-with-docs) — worked fine by treating blue+gray as "two neutral tones" and reserving status color for branches, not badges, but the doc doesn't say this explicitly.
2. The "dead-end termination" pattern (dashed line trailing into nothing) and "status-coded path" pattern (solid line, colored by outcome) aren't clearly distinguished for the case where a divergent branch *does* land in a real, labeled terminal box (not just trailing off) — had to judgment-call solid+coral for this test's "not ready yet" stub.

All 7 items on the original battle-test task list are now done: flowchart, ERD, comparison, UI mockup, illustrative, playwright render pass, findings report.

### Work order agreed this session

Build sub-skills first, in roughly this order, then come back to finalize `brainstorming` (since it depends on all of them):
1. ~~**Visualization merge**~~ — **DONE (session 3)**, see "Visualization — merge complete" above.
2. ~~**Context-capture skill**~~ — **DONE (session 4)**. Skill at `new-workflow/new-skills/context-capture/SKILL.md`. Key decisions: passive + user-invocable; open-ended triggers (agent judgment, not a fixed checklist); defaults table (decisions/notes/backlog/preferences/session.md); adaptive checkpoint format; hard rule that project info goes in repo not auto memory; lazy file creation; defers to existing project structure.
3. ~~**Assumption-check rule**~~ — **DONE (session 5)**. Saved to `new-workflow/hard-rules.md` (staging area for always-active rules that will go into the new-workflow's eventual CLAUDE.md). Rule: separate observations from inferred causes; every causal claim must be labeled as a hypothesis with a verification step ("Hypothesis: X. To verify: Y.").
4. **`write-spec` skill** — **DRAFT EXISTS, needs proper brainstorm + rewrite.** A draft was written prematurely at `new-workflow/new-skills/write-spec/SKILL.md` before the brainstorm was completed. Brainstorm was in progress (first question posed: does write-spec require brainstorm.md to exist, or can it work from conversation context alone?) when compaction hit. Resume the brainstorm from scratch — discard the draft, design properly, then rewrite. Key inputs already gathered: Superpowers brainstorming/SKILL.md spec section (self-review checklist, user review gate) and spec-document-reviewer-prompt.md (Completeness/Consistency/Clarity/Scope/YAGNI). **Next up.**
5. ~~**`brainstorming` skill**~~ — **DONE (session 5)**. Fully rewritten at `new-workflow/new-skills/brainstorming/SKILL.md`. Claude Code only (no web references), full decision capture per branch (not one-liners), grilling principles folded into Phase 2 (relentless, codebase-first, dependency-ordered walk, probe vague responses), Phase 4 hands off to write-spec. Hard rule added: never print tree + walk first branch in same message.
6. Resume the original mechanical git-mutation-strip walk for the remaining superpowers skills (writing-plans, executing-plans, finishing-a-development-branch, subagent-driven-development, using-git-worktrees, test-driven-development, systematic-debugging, verification-before-completion, requesting-code-review, writing-skills) — paused mid-walk, can resume any time, doesn't block the architecture work above.
7. `new-workflow/superpowers/CHANGELOG.md` — start this and backfill as each decision above gets actually executed (not just agreed).
8. Rewrite root `CLAUDE.md` per "Root CLAUDE.md direction" (still valid, unchanged from session 1).
9. Still-open, non-blocking: plugin final location (new repo vs. folder vs. elsewhere), install target (replace Superpowers entirely vs. sit alongside).

### agentic-workflow_v2 — template structure + executing-plans (2026-07-14, sessions 6–7)

**Pivot: primary deliverable is now `agentic-workflow_v2/`**, a self-contained starter-repo template, not a collection under `new-workflow/`. Skills live in `agentic-workflow_v2/.claude/skills/`. The `new-workflow/new-skills/` folder was deleted (user deleted after confirming all skills copied to v2).

**Skills in `agentic-workflow_v2/.claude/skills/` (all built):**
- `brainstorming/` — SKILL.md + write-spec.md
- `context-capture/` — SKILL.md
- `research/` — SKILL.md
- `research-evaluation/` — SKILL.md
- `visualization/` — SKILL.md + references/ + examples/
- `writing-plans/` — SKILL.md (updated this session — see below)
- `executing-plans/` — SKILL.md (new this session)

**Other files in `agentic-workflow_v2/`:**
- `.claude/settings.json` — git mutation deny rules
- `.claude/agents/haiku-worker.md` — static Haiku subagent instructions (new this session)
- `scripts/tree.sh` + `scripts/merge-files.js` — utility scripts (merge-files extended this session with `:N-M` line range syntax)
- `CLAUDE.md` — project template (updated with batch-checks rule + merge-files/parallel-read rule)
- `recommended-tools.md` — catalog of optional external skills
- `docs/work/now.md` + `docs/work/roadmap.md` — stubs

**Executing-plans skill decisions (extensive brainstorm, now built):**

- **Two modes:** Delegate mode (default) and Inline mode
- **Delegate mode:** Haiku subagent executes tasks with exact code specified. Main agent dispatches with ~40-token Agent call (references files by path, does NOT generate brief content inline)
- **Brief construction:** Main agent passes `offset`/`limit` to point Haiku at exact plan section. Haiku reads `.claude/agents/haiku-worker.md` + plan section + ≤3 source files. Main agent Sonnet output per dispatch: ~40 tokens
- **Haiku file read budget:** soft limit 5 total (2 for instructions + plan, 3 for source files)
- **Task classification:** DELEGATE = step includes complete exact code. INLINE = requires discovery/judgment. Plan marks INLINE tasks with `<!-- INLINE -->` comment
- **Haiku error handling:** obvious fix → one attempt, then report. Non-obvious → NEEDS_DEBUG immediately, no guessing
- **Debug agent:** Sonnet, `run_in_background=True` (FleetView visible + interactive). Receives full context brief. User notified when spawned
- **Verification:** always one chained Bash call with `&&` (never separate calls) — hard rule in CLAUDE.md
- **Progress tracking:** Edit plan.md to mark `[x]` after each verified task

**merge-files.js extension:** Added `:N-M` line range syntax to path args (e.g., `plan.md:45-89`). Ranged entries skip the `--ext`/`--except` filters (explicit inclusion). Opener label shows `file:N-M` to indicate slice.

**CLAUDE.md additions:**
- `Batch sequential checks` hard rule
- Parallel Read (≤4 files) vs merge-files (5+) guidance
- merge-files `:N-M` syntax documented

**writing-plans update:**
- New "Delegate-Ready Tasks" section: tasks must include exact code, exact file paths, explicit verification command
- Tasks needing discovery marked `<!-- INLINE -->`
- Plan header updated to reference delegate/inline distinction

**Study case analyzed:** `temp/study-cases/handy-workspaces/` — SDD-driven execution of a 9-task feature. Root failure: ~1.1M tokens, 90 min for a feature that should have taken 15 min. Caused by per-task implement+review subagents (each starting cold), final whole-branch review (90k tokens), mega fix-agent (124k tokens). Informed the entire executing-plans design.

---

## On resume

**Current state: `agentic-workflow_v2/` is the primary artifact.** Skills built (7): brainstorming (+write-spec.md), writing-plans, executing-plans, context-capture, research, research-evaluation, visualization.

### ACTIVE THREAD (2026-07-17, session 9) — Skill & knowledge ecosystem (#7)

**The current live design work is `new-workflow/design-skill-ecosystem.md` — read it first on resume.** It designs the reusable, growing, publishable knowledge system that sits alongside v2's process skills (the "I always lose/miss conventions" problem). Grounded in the user's own reference implementation `temp/debug-web-pages/`.

**Corrected core model (confirmed):** the **skill is the atomic unit** of reusable/growing/publishable knowledge; its internal `knowledge/` folder IS the cross-project knowledge base (no separate store). Reusable → a skill; the rare project-only thing → project docs (`docs/spec/decisions`). Workflow and skills are separate publishable artifacts.

**Branches CLOSED (#1–#4) — full detail in the ecosystem doc's LOCKED sections:**
- **#1 — skill anatomy standard.** Thesis: *standardize the NAMES of the pieces, not WHICH pieces a skill has.* One hard invariant = `SKILL.md`; everything else (`knowledge/`, `tools/`, `MAINTAINING.md`) **optional-by-purpose, no thresholds.** Two heuristics: smallest-shape-that-works + load-frequency split (`SKILL.md` lean+stable+always-loaded / `knowledge/` on-demand). `DESIGN.md`/`ROADMAP.md` = WIP-only scratch, NOT anatomy; only `MAINTAINING.md` stands. Knowledge layout **emergent** (shape-library: instance-cache / topic / recipe / flat-note — OPEN, not a fixed taxonomy). `_TEMPLATE.md` scoped to instance-cache ONLY, NOT domain-agnostic. `debug-web-pages` is ONE shape, not the template.
- **#2 — growth/maintenance framework.** *Part A:* maintenance mechanics hoisted into ONE reusable skill **`maintaining-skills`** (NEW, two-mode: **Grow** = write/promote/prune; **Audit** = check current work against a skill's best-practices). Per-skill `MAINTAINING.md` demoted to THIN config the meta-skill reads. Audit lives HERE, not in `verification-before-completion` (which isn't in v2 yet + is milestone-scoped; audit is skill-scoped/cross-cutting; future: verify MAY *call* audit but doesn't own it). *Part B (the janitor's 5 rules, plain-language):* 1 don't-write-twice→move-to-shared; 2 delete-junk-aggressively (git is the archive); 3 route-new-knowledge (general→shared / specific→that file / new-ability→tool + one line in `SKILL.md`); 4 write-only-confirmed (dated, sourced, stable-locator, guesses in "open questions"); 5 keep-notes-short (positive, single-source, leading-words).
- **#3 — capture/routing loop** (the "I miss things" fix). Split **Notice** (free — one line into a file during work, rides on `context-capture`) from **Sort** (later, at a stopping point). The reusable-vs-project question is NOT a real fork (nearly everything is reusable) → **flip the default → a skill**; rare exception → project docs. **NO global inbox** (rejected) — a note with no skill yet → create a local stub skill on the spot. Skills are **local + git-tracked in a clone you own** so edits are visible/reviewable.
- **#4 — publishing/packaging** (CLOSED 2026-07-18). **Load-bearing correction:** the pick-skills/pick-agent/pick-scope installer the user liked is `npx skills add` (**`vercel-labs/skills`**), NOT Claude Code's native plugin install (which is **all-or-nothing per plugin**, `/plugin marketplace add` → `/plugin install name@mkt`). A repo can satisfy both at once (mattpocock does). **Decisions:** (1) **two repos** — Repo A = workflow template (`agentic-workflow_v2`: CLAUDE.md + docs scaffold + the core "how to work" skills), Repo B = skills catalog (stack/tool knowledge skills, grows forever, installed piece-by-piece); (2) **no git submodules** — two clones side-by-side in one local folder, template references catalog by install-command/URL; (3) **core skills live INSIDE the template (Option 1)** — they're part of the workflow, version with CLAUDE.md, not external add-ons; catalog holds only stack/tool skills; (4) **link, never hard-copy** — symlink from a clone so `git pull` updates everywhere (copies are frozen; `--copy` stays available for strangers); (5) **ship both doors** — catalog is `npx skills`-installable AND carries `marketplace.json`; engine is a whole-bundle plugin; (6) **release discipline deferred** — no version numbers (Claude Code treats no-version as "every commit = latest"); add explicit versions + changesets only once strangers install.

**Global-repo model (mechanics now LOCKED in branch #4 above):** hosted install-FROM source, mattpocock-style (`new-workflow/skills` = the reference) — clone → `npx skills add` picker → land local (symlinked) → edit in the git-tracked clone (edits visible) → `git push` promotes worthwhile edits UP → others `git pull`/`skills update`.

**NEXT ACTION:** walk ecosystem-doc **branch #5 — init-integration seam** (at `project-init`: read the tech spec → recommend/import relevant skills, external + personal → scaffold project-local capture; touches genesis-doc #4/seeding). Then **#6** skill-creation trigger ("no skill yet → make one"; who authors it — `write-a-skill` / `superpowers:writing-skills`). That closes the ecosystem thread.

**Front-of-lifecycle thread (`design-project-genesis.md`) — essentially CLOSED this session:** locked 3.2 technical core (a engine / b what-"resolved"-means / c research-plug-in+`docs/research/` storage), 3.3 interaction (4-phase A–D walk), 3.4 working memory (`docs/work/consolidation.md` + bootstrap now.md + context-capture), renamed skill **project genesis → `project-init`**, closed #4 derivation (Phase E), closed **#8 now.md = thin cursor**. Only #6 skill-topology remains parked there.

**Mode reminder:** plain conversational brainstorming — do NOT invoke `superpowers:brainstorming` or the v2 `brainstorming` skill on this meta-work. Commit to a position per branch, one branch at a time. `framework-build/` + `temp/debug-web-pages/` are read-only reference; do NOT reference the deprecated v1 `project-init.md` (the new `project-init` is a fresh design reclaiming the name).

### Skills still to build (after the design threads close)
- **`debugging`** — mid-execution debug flow. Needs brainstorm. Framework-build D52-D53 has design notes (Phase 0 calibrate + 5 adaptations of diagnosing-bugs). Hard rule "never state cause without evidence" already in CLAUDE.md.
- **`verification`** / **`finishing-topic`** — needs brainstorm (may be 1 or 2 skills)
- **`project-init` skill** itself — build once the genesis design closes (design is essentially done; #6 topology parked).
- **Skill ecosystem** (#7) — design in progress (`design-skill-ecosystem.md`), branches #1–#3 closed; implementation after design closes.
- **`maintaining-skills` skill** (NEW, confirmed session 9) — the two-mode (Grow + Audit) reusable meta-skill that maintains other skills' knowledge; content = branch #2's 5 rules. Build after the ecosystem design closes.
- **Architecture skill — INVESTIGATE (low priority, 2026-07-18):** evaluate mattpocock's `codebase-design` vs the installed `improve-codebase-architecture`; decide adopt / reuse-one / build-own. Likely lands in the global skills catalog (Repo B), not the template. Surfaced while pruning `recommended-tools.md` — deliberately kept OUT of that file until decided. Not a commitment.
- **Plugin registration** — how to get `flow:` prefix working with local install. Deferred.
- **CLAUDE.md final content** — deferred until all skills exist. Note: the `## Session start` section was **deleted** (2026-07-27) along with `now.md` — it said only "read `now.md` for the active topic," so nothing was left once the file went. Session start gets rewritten with the `handoff` design. *(Supersedes the earlier note about tweaking that wording for #8's thin-cursor `now.md`.)*

### `handoff` — BUILT (2026-07-27), two follow-ups open

Skill written from scratch and locked: replaces `/compact`; default job is **resume**, an argument aims it, naming a different job makes it a brief; the skill specifies **coverage, not layout** (document shape follows the work); the read-list with verified paths + line ranges is the load-bearing part; **written automatically whenever a phase closes** (brainstorm/spec/plan/execution/milestone), which makes the file double as the always-current status file — this is what replaced framework-build's separate `context.md`; the document carries its own two boot lines because the reader usually won't have the skill loaded.

- **`PreCompact` hook — build it.** Verified against the Claude Code hooks docs (2026-07-27): `PreCompact` fires before compaction, supports a **matcher on `auto`** vs `manual`, and **can block** (exit code 2, or `{"decision":"block","reason":...}`); on exit-2 the stderr text is fed back to Claude as an error, and it can also inject `additionalContext`. So auto-compaction can be replaced rather than suppressed: block on `auto` → the message reaches the agent → agent runs `handoff` → user clears. **Must block only once per session** (state file), letting a second auto-compact through as the fallback — otherwise a failed handoff strands the session at the context wall with no compaction. Note it fires *late* by design, so it is the safety net; the phase-close trigger inside the skill is the primary path. Deliberately kept out of `SKILL.md` until it exists.
- **Milestone-level handoff path — blocked.** The skill ships the topic-level convention (`docs/work/topics/t<NN>-<slug>/handoff.md`, else `docs/work/handoff.md`). Where a *milestone's* handoff lives — and whether finishing milestone N writes the seed for N+1 into N+1's folder — waits on topic-folder structure, which `design-project-genesis.md` #8 defers to the middle-of-lifecycle design.

### TWO NEW DESIGN THREADS opened 2026-07-27 (study-case driven)

Five study cases read (`tmp/study-cases/`), issues defined, two design docs created. **The explain thread is BUILT (2026-07-28); the brainstorm thread is still open.**

- **`new-workflow/design-brainstorm-rework.md`** — the `brainstorm` skill has one exit sized for one buildable thing, so anything bigger strands its upper-altitude decisions (evidence: `read-aloud-app/case3`, 1069-line brainstorm → 309-line phase-1-only spec; Branch E, A2/A3, phases 2–3 and the adapter-surface direction all left with no downstream home). **LOCKED:** two modes only (full-product / topic-feature); full-product produces a multi-file **product foundation, not a spec**; that foundation IS `docs/spec/` as already designed in `design-project-genesis.md` #1; **no `intake/` folder on this path** (nothing to intake — the material is generated in-session); topic mode keeps brainstorm→spec→plan; the **phase cut must be explicitly documented** (brainstorm.md is an acceptable home) instead of stranded in the session; delivered as a sibling sub-file to `write-spec.md`. **OPEN:** the actual file set for the product foundation (the hard one), mode selection, engine for full-product mode, overlap with `project-init`, whether it ends by deriving CLAUDE.md+backlog.md, sub-file name. Plus four unblocked smaller fixes (kill the resume pointer — that's `handoff`'s job now; rewrite superseded sections instead of layering corrections; research/test scaffolding → `docs/research/`; resolve the deferred-vs-out-of-scope contradiction between SKILL.md lines 120 and 138).
- **`new-workflow/design-explain-rework.md` — DONE, built 2026-07-28.** All three logged `explain` failures happened **without the skill firing**. The fix was not a better trigger: at 70–90% of user-facing output `explain` was a **mode**, not an on-demand skill, and a mode fails when the model never recognizes it's in one — recognition precedes fetching, so fetching can't repair it. **The split is by medium.** Prose rules (whole picture first, define from zero, no undefined shorthand, priority order, no preamble, final message written for the user, UI rendered not described) moved to `flow/CLAUDE.md` `## Explaining` and now govern every answer including status reports. The skill keeps only rendered-artifact machinery and is **no longer ambient** — which at the time left `note` as the single ambient skill; `note` itself dissolved the next day (see the capture thread below), so nothing is ambient now. Also locked: one file, do not split; ~300 lines fine / ~500 acceptable; ASCII-first; verification is a **shipped script** (`explain/scripts/check-frame.js` — explicit allowlist, not Unicode width tables, because `━` is banned while `─` is fine and Unicode calls both "Ambiguous"); the "sacrifice grammar for concision" line **rejected**; scratch path is `tmp/`. `flow/CLAUDE.md` fully rewritten alongside — merged user sections back to a placeholder, new `## Preferences` (auto-memory is disabled in settings, so preferences need a file that loads without being chosen), `## Workflow` skill map, `## Scripts` as active instruction, git + auto-memory + no-placeholders rules deleted as dead weight, and the install prohibition **inverted** into two prescriptive rules (run the package manager, run the official CLI). Still open: session-start section and a key-docs table, both blocked on `project-init`.

- **`new-workflow/design-capture-rework.md` — DONE, built 2026-07-29.** Third application of the same principle, and the last ambient skill is gone. `note`'s founding rule ("never decide the destination at capture time — deciding is the tax that makes capture get skipped") was **conditional stated as absolute**: the tax is *deliberation*, not writing, and when the home already exists the write costs one `Edit` either way — so a feature idea travelled `inbox.md` → `organize` → `backlog.md` for no reason. Second and stronger: `note` carried the *detection* rule ("preferences are inferred, not announced") inside a file that only loads once detection has already succeeded — **any behavior whose trigger is itself a judgment call cannot live behind that judgment call**, exactly what moved `explain`'s prose rules. **LOCKED:** `note` dissolves into `flow/CLAUDE.md` `## Capture` (22-line body, no residue worth a stub); the user's proposed `note`+`organize` merge **rejected** — the real cut is by *deliberation cost*, not topic; **two tests** to route directly (home exists now **and** item is usable there as-is), fail either → `inbox.md`, which makes the inbox the pressure valve for the unshaped exactly as the user wanted; **never route to a skill at capture time** (altitude is the hardest call and the place duplication happens); `organize` keeps altitude routing, `needs skill:` flagging, fragment shaping and the end-of-topic sweep, its table reduced to the two skill rows with the rest pointing at `## Capture`. `note/` archived to `reference/archived-skills/` pending a delete confirmation; `/note` as a typed command is accepted collateral ("note that" still works). Still open: whether `docs/spec/decisions.md` exists at capture time — blocked on `project-init` like the session-start section.

- **`new-workflow/design-init-flow.md` — OPEN, started 2026-07-29. THE ACTIVE THREAD.** Supersedes the front half of `design-project-genesis.md`. **`project-init` was one name over two jobs**: its phases A–D (assess intake → walk gaps → propose spec structure → write spec) are a multi-session research-driven branch walk that *is* `brainstorm`'s full-product mode and migrate to that thread; what's left is the front door. Same deliberation-cost cut as the explain and capture threads. **LOCKED:** renamed **`init-flow`** (verb-first; avoids the built-in `/init`; it never decides what the product is) and made **re-runnable** so it also updates an existing Flow project; **`consolidation.md` dissolved** — genesis #3.4 admits it "IS the project-altitude `brainstorm.md`", so product mode writes `docs/work/brainstorm.md` and `handoff` owns resume state; **splitting `flow/CLAUDE.md` PROPOSED AND REJECTED** — an `@` import is a mechanism that can silently fail to load, which is the exact failure the last three threads removed, it doesn't solve migration (the hazard is contradiction, and two files hide it), and the two-lifecycles framing was wrong since the project half fills over the whole project life via `## Capture`; **migration is CONVERSION, not merge** — Flow replaces the user's workflow, harvests content (stack, structure, code rules, verified commands, gotchas) mostly into `docs/spec/` and `docs/context/`, discards their process, treats their docs as intake and never consolidates them (that's an offered, declinable product-brainstorm run), merges `settings.json` additively; **blocking git mutations is a Flow default**, not a personal preference; **borrowing from `reference/mattpocock-skills` requires re-derivation** — his skills assume a tracker, GitHub, PRs and a team loop, ours assumes files and one author; **template `CHANGELOG.md`** is what makes the re-run cheap (read entries forward from the project's stamped version instead of diffing two CLAUDE.mds); **`prototype` PARKED** — the user's real need is technical **feasibility spikes**, which genesis #3.2.b already routes to a flagged first backlog item; **`brainstorm`'s core is not up for rewrite** (user: it worked, results were satisfying — the failure was at the seam). **Survey evidence (2026-07-29):** `tmp/local-refs/delapse/docs` + `lumacraft_v2/docs`, 77 files sorted by kind — Flow **deletes the biggest category outright** (process prose: `workflow-rules.md`, `planning.md`, `milestones.md`, `research.md`, `testing.md`, `superpowers-overrides.md` — that category *is* the skills, and only existed because v1 had none), and the real failure mode is **bloat, not absence** (280-line conventions, 560-line decisions log, `commands.md` "bloated"). **PROPOSALS accepted by silence:** `docs/context/` for project-specific durable knowledge that is neither rule nor decision nor skill, with four anti-bloat rules (what would a fresh session get wrong without this / facts not process / verified only / rewrite don't append) — this also unblocks the key-docs table, which must list only stable paths; the **skill-vs-context test** — *would this sentence be true in a different project?* yes → skill (`model-notes.md` is portable LLM-pipeline knowledge, not project docs), no → `docs/context/`, **in-between content splits into a portable rule and a local fact rather than being assigned**, and when genuinely unclear it stays in the inbox; **`setup-notes.md` is a pipeline not a destination** (inbox → `curate-skills` → skill); **no generic `testing` skill** (stack half → catalog skill, project half → `docs/context/`); topic **status via YAML frontmatter + `scripts/status.sh`**, because derived state can't go stale the way lumacraft's hand-kept `now.md` table did. **Also shipped:** two `flow/CLAUDE.md` rules — *Read the least that answers the question* (leading `## Scripts`) and *Reason before agreeing* (in `## Communication`). **Verified closed:** script documentation in `## Scripts` is already complete against both script headers. **Locked 2026-07-30 — standing principle: the only user is the author.** Nothing gets designed for strangers; anything that only earns its keep with a third-party audience is demoted, not kept just in case (same trigger as ecosystem #4.6). Applied immediately: **#7 template changelog DEMOTED** (`git log` on `flow/` is the changelog for one user) and **#D version stamp CLOSED as no-stamp** (it existed only to index into that changelog). Also locked: **#9 payload delivery — fetch `flow/` at run time** (shallow clone to `tmp/`, copy out, discard); bundling the payload inside the skill is **rejected** (duplicates `flow/` in `flow-skills/`, they drift); consequence is that *Use this template* stops being the door — every path is `npx skills add` then `init-flow`, and link-never-hard-copy doesn't apply because payload files are edited per project. **#10 conflicting paths are quarantined, not merged** — existing material that collides with Flow's structure moves into `docs/intake/` first, Flow's layout lands on the cleared slate, and the run then works from the quarantined copy; `docs/intake/` rather than a gitignored path because moving tracked files into an ignored folder is a silent delete; nothing deleted, origins recorded, move reported. **No exceptions — `.claude/settings.json` is quarantined too** (user corrected the merge-additively carve-out: the file carries hooks, MCP servers, env, statusline and model config, not just an allow/deny list, so merging = negotiating with arbitrary foreign config). Flow writes its own, harvests facts back (their MCP servers, permission entries for their tooling), drops their hooks as process. **`settings.local.json` is not touched** — gitignored personal state, and moving it into a tracked `docs/intake/` would commit machine-local config; read once and reported if it overrides a Flow key, since a local permission can silently undo the git deny list of #5. Settings apply next session (loaded at startup), so `init-flow` closes by telling the user to restart. **Ecosystem #5 (init integration seam) — CLOSED 2026-07-30, no seam survives.** Its three clauses: *scaffold project-local capture* is **dead** (capture lives in `flow/CLAUDE.md` and `inbox.md` is created on first write — it arrives with the payload); *recommend from the tech spec* is **deferred** — it never applied to the process skills (mandatory, already enforced by `check-skills.sh`), only to stack skills, and the catalog holds **zero** of those (all eight are process/domain), so it's a recommender over an empty candidate set; *external skills* **dropped** (would mean carrying a registry of other people's work). **`init-flow`'s skill step is a check, never an install** — skills are globally symlinked on the author's machine, and Flow's hard rules forbid running installs; missing → print `npx skills add Adrian333Dev/flow-skills`. **Found while walking:** `check-skills.sh`'s required list is stale (`note` dissolved; `handoff` + `curate-skills` missing) → backlog. **#11 payload boundary LOCKED** — of `flow/`'s nine files, **six ship** (`CLAUDE.md`, `.claude/settings.json`, `scripts/×3`, empty `docs/work/backlog.md`), **`.gitignore` appends** the `tmp/` line only, and **`README.md` + `recommended-tools.md` never ship** (the README describes the template to an evaluator and would overwrite theirs; recommended-tools is human-facing advice identical everywhere that would rot in N copies). No empty `docs/` folders are scaffolded — everything is created on first write. **`.gitignore` is the one true exception to #10's quarantine** (theirs holds `node_modules/`, `dist/`, `.env` — replacing it would start tracking all of it). **#12 harvest boundary LOCKED — `CLAUDE.md` + `docs/context/` only, never `docs/spec/`**, resolving a contradiction inside #4 (it said both "most harvest lands in `docs/spec/`" and "`init-flow` never consolidates their docs" — writing the spec from their docs *is* consolidation). Same deliberation-cost cut: lifting a code rule or a verified command is mechanical and item-checkable; synthesizing a spec means reconciling documents written months apart and deciding what the product is now, which left with phases A–D. Second reason: `docs/spec/` is what `CLAUDE.md`/`backlog.md` derive from, so a half-harvested spec is worse than none. **Three harvest destinations:** code rules → `## Project rules`; verified commands → `docs/context/commands.md` (existing repos only); other durable facts passing #A's four rules → `docs/context/<subject>.md`; everything else stays in `docs/intake/`. **Knock-on — closes the capture thread's parked question:** `docs/spec/decisions.md` does not exist after init, and `## Capture`'s "where that exists" wording is already correct. **#A and #A2 marked ACCEPTED; #C SETTLED.** Design thread is closed — **NEXT: build `flow-skills/skills/init-flow/SKILL.md`**, register it in `plugin.json` + README, fix the stale `check-skills.sh` list, and add the key-docs table + `docs/context/` routing to `flow/CLAUDE.md`.

- **`new-workflow/backlog.md` — NEW 2026-07-29.** The workbench's own loose-item list (skills to build: `code-review`, an `ask-matt`-shaped router, testing-skill decision; reworks: `explain` mockups, `debug-web-pages`, telegraphic pass; hooks: `PreCompact`, context-pulse; migration of `delapse`/`lumacraft_v2` as the real test cases; the `reference/archived-skills/note/` delete). **Convention locked this session: a proposal the user doesn't answer is accepted** — don't park approved work in a "waiting on you" list.

**`project-init`'s scope change is now DONE** — it is the `design-init-flow.md` thread above. `design-project-genesis.md` is superseded at the front half; its phases A–D belong to `design-brainstorm-rework.md`, closing that doc's #D (overlap with project-init) and genesis #6 (skill topology).

**Note:** `write-spec` is NOT a missing skill — it already exists as Phase 4 of `brainstorming` (`skills/brainstorming/write-spec.md`). Earlier notes calling it a gap are outdated.

**Pending mechanical fix (awaiting go):** `agentic-workflow_v2/docs/work/roadmap.md` → `backlog.md` (framework-build D16: no roadmap, flat backlog). Also (from #3.2.c): a future `docs/research/` folder convention for external research reports (`<NN>-<slug>.md`, prompt+report together, referenced not inlined).

**Do NOT touch `framework-build/` files (read-only reference). Do NOT reference the deprecated v1 `project-init.md`.**

_Updated: 2026-07-17 (session 9)._

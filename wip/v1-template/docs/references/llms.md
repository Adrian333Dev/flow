# LLM-ready docs

Optional. Use this file to override `context7` for specific libraries when you've found `context7`'s docs to be stale or insufficient.

| Tool | llms.txt | llms-full.txt | Notes |
|------|----------|---------------|-------|
| <!-- tool name --> | <!-- URL --> | <!-- URL --> | <!-- version, reason for override --> |

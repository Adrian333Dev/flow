# References

External resources for this project. Files here are optional — create them only when you need them.

| File | Content |
|------|---------|
| `llms.md` | Optional `llms.txt` / `llms-full.txt` overrides for specific libraries when `context7` is stale |
| `links.md` | External dashboards, service consoles, runbooks, design files (create as needed) |

ADR / decision logs typically live in `docs/spec/` since they track product / architecture choices. Keep one here only if you have purely external/operational decisions worth separating.

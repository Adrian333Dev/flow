# Setup Notes

One-time decisions and scaffold choices made during project initialization. This is a **reference**, not a directive — these are things that were done once, not rules to follow on every file.

> Populated by `/project-init` and updated as scaffolding decisions are made. If a decision is already baked into a config file, it belongs here (so future agents don't re-litigate it). If it's a pattern to apply to every new file, it belongs in `conventions.md`.

---

<!-- Add sections as scaffolding decisions are made. Examples:

## Build tool / monorepo

- Turborepo pipeline: ...
- pnpm workspace config: ...

## Package manager

- Version: pnpm X.Y
- onlyBuiltDependencies: ...

## Type generation

- DB types generated via: ...
- Output path: ...

## Bootstrap ordering

- Middleware registration order: ...

## Migration conventions

- One migration per milestone. Header comment with purpose + milestone ref.
- Trigger function defined once, reused: ...

-->

{
  "name": "flow-scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Flow's scripts, and the tests that run them. No dependencies: node --test is built in.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}

# {{title}} — groundwork

<!-- Empty until this ticket is picked up. That is normal, not a gap: the folder
     exists from birth so its location never depends on how the groundwork turns out.

     Every branch and every decision lives in this one file: a markdown checklist
     of branches, nested as deep as the subject needs, each walked to resolution,
     then one section per resolved branch below it. A branch that actually grows
     gets its own file beside this one.

     Every leaf is a question. Group headings gather them on a big subject, and
     are written in Title Case so the two read apart on sight.

     - [ ] 0 — A Group Of Questions
       - [x] 0.0 — a settled question?
       - [ ] 0.1 — one that surfaced while walking its parent?
     - [ ] 1 — a question standing on its own?

     A `## References` section at the bottom collects what the build will have to
     read, a line added the moment it is found. Phase 4 splits it across the
     tickets this map produces.
     -->
